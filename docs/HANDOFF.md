# CapitolWonk — current handoff

Checkpoint: **September 16, 2026, 2:08 p.m. PDT**. Milestone: October 30 launch preparation; Apple signing and verifier acceptance remain critical. Read [`AGENTS.md`](../AGENTS.md), [`PROJECT-CONTEXT.md`](PROJECT-CONTEXT.md), active [`DECISIONS.md`](DECISIONS.md), and the newest [T01–T11 ledger](project-timeline.md) section before work. The [dated EOD](eod-handoff-2026-09-16.md) archives the full standing rules.

## Exact workspace and preservation state

- Active local checkout: `/Users/tylergates/Documents/Capitol Ledger/.worktrees/sept16-t09`; branch `codex/sept16-t09`; `HEAD` `93795c7b59f2df4af49dee28ff3b8817e973d51a`. It was cloned locally from the saved project and its `origin` URL now points to GitHub, but no fresh remote fetch or push has occurred. It shares existing Git objects with the saved project, so preserve the parent repository. The saved checkout itself remains on old `main` at `b2e11b4`; its full status scan stalled, and no tracked file there was overwritten.
- Source worktree `/Users/tylergates/.codex/worktrees/427d/Capitol Ledger` remains intact on `codex/member-service-live-fallback` at the same feature commit. Its 12 local/uncommitted September 16 EOD documentation files were copied byte-for-byte into this checkout before further edits. The five new continuity files, seven modified EOD files, and this continuation's T09 documentation are preserved on this local branch; no push or PR has occurred for them. Verify `git status` and `git log` for the current branch state. Do not mistake this checkout for `main` or assume the PR #31 merge object is locally fetched. The source handoff recorded [PR #31](https://github.com/Tylerandersongates/Capitol-Ledger/pull/31) merged as `90f8b85f6818ca8fdb11ce9e678d6d37439880e7`; its head showed 3/3 checks and its merge event displayed 2/3 when observed.
- No new secrets or protected values are recorded here. OS 27 is installed. `node_modules`, `.next`, repo pnpm stores, and specified Xcode DerivedData were intentionally cleared for disk space. Reinstall/rebuild only for an actual task; missing caches are not missing source. No provider/environment setting was changed during this EOD.

## Verified work and evidence boundaries

- September 15 [PR #29](https://github.com/Tylerandersongates/Capitol-Ledger/pull/29) aligned the Oct 30 critical path. [PR #30](https://github.com/Tylerandersongates/Capitol-Ledger/pull/30), merge `f0bd3aa77a1089ae07ca72a0ffd7bc2d7f196a80`, is merged with three checks reported at merge; it corrected mailbox-only privacy copy and updated the local App Privacy/four-screen capture plan. No App Store Connect or screenshot upload occurred.
- This continuation closed one local T09 documentation gap: the [provisional listing/capture-state draft](app-store-assets/capitolwonk-listing-draft-2026-09-16.md) records honest public routes, copy candidates, and corrected-wordmark checks. The capture manifest links it; the setup packet now uses canonical URL candidates, removes a demo bill screenshot ID, and marks its old App Review text as historical. No actual candidate image or remote record was changed.
- [PR #31](https://github.com/Tylerandersongates/Capitol-Ledger/pull/31) fixed live Congress.gov member election-history fallback before caching, including the database-timeout path, and added `scripts/check-member-service-history.mjs`. The prior session ran that guard and four neighboring member guards. Prior-session Vercel production Ready and live `/members/S001150` smoke displayed **First Elected Nov 5, 2024** and **Next Election Nov 5, 2030**. Those are prior-session checks against the deployed merge, not tests rerun during this EOD. Do not infer all member records are correct from one smoke.
- EOD-only validation on the documentation state: the bundled Node runtime ran `scripts/check-eod-standing-rules.mjs` successfully (template, prompt, and three dated EODs); `git diff --check` passed; a targeted trailing-whitespace scan of new continuity files found no matches. No app build or broad feature test was run for this handoff.
- Production Batch A and the reviewed default-off source were completed earlier. The dedicated mailbox is the launch privacy-intake channel. First-party intake, operations, monitoring, retention, deletion, adapter, App Store server verification, and Notifications V2 are **off**. Source-only packets and a Ready deployment do not activate them.

## Open, blocked, and deferred

- **T03 high/open:** Apple verifier OCSP issue #447, residual `jsrsasign` acceptance, independent review, and signed/sandbox/device evidence. Keep processing off. Official-fix watch is a safe read-only task; the September 25 decision checkpoint remains.
- **T04 blocked:** Tyler relayed interim Apple Support guidance September 16: a revoked certificate must be replaced and the application package signed with the replacement; Apple also sent a generic Keychain Access request guide. Keychain Access shows two Apple Development certificates as untrusted, with a private key under each and system-default trust settings, while Certificate Assistant's Code Signing evaluation succeeds for both. The signed-in Developer portal still lists the name-labeled Development certificate as active in the selected team alongside its Distribution Managed certificate; the email-labeled certificate's other-team status is unknown. A normal-access read-only command sees **2 signing identities, 0 valid**; the earlier zero-total result came from sandboxed visibility. Xcode signing usability, cause, and effect on existing Distribution/profile assets remain unresolved. The [signing record](apple-signing-reconciliation-2026-09-10.md) distinguishes the reply and read-only evidence from a tested remedy. Preserve the certificate/CSR/private-key/Keychain/profile/signing/device freeze pending a reviewed exact action and Tyler's approval. No usable Xcode signing identity or signed release candidate has been evidenced.
- **T05–T07** require a valid signed/device candidate for native monitoring, physical-device QA, and Apple sandbox subscription matrix. **T08** first Daily Brief/video launch scope and inputs are undecided. **T09** App Privacy answers, corrected-wordmark listing assets, truthful mailbox procedure, and read-only provider/retention evidence are incomplete; final answers and remote edits require separate gates. **T10–T11** TestFlight, review, and release are not authorized. See the [ledger](project-timeline.md) for owners, effort, dependencies, and dates.
- The production ambient-privilege preflight failed closed; the later principal-ACL inventory never ran. During its preparation a Neon owner credential was exposed locally, then reset by Tyler with containment closed. Do **not** retry the inventory or revive the operator database sequence as a launch task. See the [sanitized incident record](neon-owner-credential-containment-2026-09-15.md). The first-party operator path is conditional post-launch unless Tyler explicitly changes scope.

## Next authorized actions

1. **First:** Verify this active checkout's path, branch, `HEAD`, and full status; preserve this local branch before switching or merging. The September 16 source worktree remains a separate intact fallback.
2. Continue T09 with read-only verification of the canonical public support/privacy pages and provider/retention evidence. Later rehearse the four public capture states against an exact candidate, recording real data provenance and corrected `CAPITOLWONK` branding; no final image or questionnaire claim follows from the local draft alone.
3. Continue the official Apple verifier-fix watch. For T04, obtain Apple's certificate-specific instructions on the existing case, then compare them with local zero-identity and current-profile evidence and present one exact action to Tyler. If the interim reply remains generic at the September 17 trigger, prepare a sanitized same-case clarification for Tyler's approval; do not send it automatically. Assess T08 scope with Tyler only when a real product choice is needed. Do not run production ACL inventory, sign, upload, distribute, submit, or release without the corresponding exact gate.

The October 30 target is unchanged but low-confidence/materially at risk. Keep October 2–6 free of required Tyler approvals/device/upload/submission work and preserve October 20–29 review/rework. This handoff does not replace the full ledger or the exact Standing Rules.
