# CapitolWonk — current handoff

Checkpoint: **September 16, 2026, late afternoon PDT**, after the approved Keychain repair, one-iPhone registration, Development-profile creation, verified Xcode 27 signed Release archive, and approved in-place build `2` device install/first launch. Milestone: October 30 launch preparation; signed-in device/sandbox proof and verifier acceptance remain critical. Read [`AGENTS.md`](../AGENTS.md), [`PROJECT-CONTEXT.md`](PROJECT-CONTEXT.md), active [`DECISIONS.md`](DECISIONS.md), and the newest [ordered T01–T11 ledger](project-timeline.md) section before work. The [dated EOD](eod-handoff-2026-09-16.md) archives the full standing rules.

## Exact workspace and preservation state

- Active local checkout: `/Users/tylergates/Documents/Capitol Ledger/.worktrees/sept16-t09`; branch `codex/sept16-t09`, with local documentation commits above source commit `93795c7`. Verify current `HEAD` and status before changing branches. Its `origin/main` was fetched to merged PR #31 at `90f8b85`; app/native/dependency/script files match this checkout. No push or PR was made for the continuation. It shares existing Git objects with the saved project, so preserve the parent repository. The saved checkout itself remained on old `main` at `b2e11b4`; its full status scan stalled, and no tracked file there was overwritten.
- Source worktree `/Users/tylergates/.codex/worktrees/427d/Capitol Ledger` remains intact on `codex/member-service-live-fallback` at the same feature commit. Its 12 local/uncommitted September 16 EOD documentation files were copied byte-for-byte into this checkout before further edits. The continuity, T09, and T04 documentation is committed locally on this branch. Verify `git status` and `git log` for its current tip; do not mistake this checkout for `main`. The source handoff recorded [PR #31](https://github.com/Tylerandersongates/Capitol-Ledger/pull/31) merged as `90f8b85f6818ca8fdb11ce9e678d6d37439880e7`; its head showed 3/3 checks and its merge event displayed 2/3 when observed.
- No new secrets or protected values are recorded here. OS 27 is installed. `node_modules`, `.next`, repo pnpm stores, and specified Xcode DerivedData were intentionally cleared for disk space. Reinstall/rebuild only for an actual task; missing caches are not missing source. No provider/environment setting was changed during this EOD.

## Verified work and evidence boundaries

- September 15 [PR #29](https://github.com/Tylerandersongates/Capitol-Ledger/pull/29) aligned the Oct 30 critical path. [PR #30](https://github.com/Tylerandersongates/Capitol-Ledger/pull/30), merge `f0bd3aa77a1089ae07ca72a0ffd7bc2d7f196a80`, is merged with three checks reported at merge; it corrected mailbox-only privacy copy and updated the local App Privacy/four-screen capture plan. No App Store Connect or screenshot upload occurred.
- This continuation closed one local T09 documentation gap: the [provisional listing/capture-state draft](app-store-assets/capitolwonk-listing-draft-2026-09-16.md) records honest public routes, copy candidates, and corrected-wordmark checks. The capture manifest links it; the setup packet now uses canonical URL candidates, removes a demo bill screenshot ID, and marks its old App Review text as historical. No actual candidate image or remote record was changed.
- [PR #31](https://github.com/Tylerandersongates/Capitol-Ledger/pull/31) fixed live Congress.gov member election-history fallback before caching, including the database-timeout path, and added `scripts/check-member-service-history.mjs`. The prior session ran that guard and four neighboring member guards. Prior-session Vercel production Ready and live `/members/S001150` smoke displayed **First Elected Nov 5, 2024** and **Next Election Nov 5, 2030**. Those are prior-session checks against the deployed merge, not tests rerun during this EOD. Do not infer all member records are correct from one smoke.
- EOD-only validation on the documentation state: the bundled Node runtime ran `scripts/check-eod-standing-rules.mjs` successfully (template, prompt, and three dated EODs); `git diff --check` passed; a targeted trailing-whitespace scan of new continuity files found no matches. No app build or broad feature test was run for this handoff.
- Current native candidate: selected Xcode is **27.0**; the Release scheme, version `1.0` build `2`, and Sentry Cocoa `9.22.0` resolved. A signing-disabled generic iOS Release build passed on the app/native source also present in fetched `main`. With Tyler's exact approval, one physical iPhone was registered in the enrolled team and one matching iOS App Development profile was generated using the existing App ID and valid Development certificate. The checked-in project remains on automatic signing; a portal-created manual profile was selected only in the successful archive command. The local archive at `/Users/tylergates/Library/Developer/Xcode/Archives/2026-09-16/CapitolWonk-1.0-2-local.xcarchive` passed strict/deep strict `codesign`, version/build, team, bundle and embedded-profile comparisons. A later sandboxed CLI signing check falsely reported untrusted/zero identities; normal macOS-access checks again found two valid identities and passed deep strict verification. Tyler separately approved an in-place update of the existing developer-installed build `1`; `devicectl` confirmed installed build `2` and an accessible container, launched it, and a one-time screenshot showed the sign-in screen. The screenshot was deleted. No credentials or account data were entered; persistence and signed-in QA remain untested. No IPA export, TestFlight or App Store upload occurred. Free space after install was about 3.4 GiB; the newly generated device-support cache was left in place. The project DerivedData remains a reusable package cache.
- Production Batch A and the reviewed default-off source were completed earlier. The dedicated mailbox is the launch privacy-intake channel. First-party intake, operations, monitoring, retention, deletion, adapter, App Store server verification, and Notifications V2 are **off**. Source-only packets and a Ready deployment do not activate them.

## Open, blocked, and deferred

- **T03 high/open:** Apple's [OCSP issue #447](https://github.com/apple/app-store-server-library-node/issues/447) remained open without a linked fix and its latest listed release was `3.1.0` on the September 16 read. Residual `jsrsasign` acceptance, independent review, and signed/sandbox/device evidence remain. Keep processing off; use the September 25 decision checkpoint rather than repeated ad hoc switches.
- **T04 local signed-archive proof complete:** Both Development identities became valid after Tyler approved importing Apple's public WWDR G3 intermediate at default trust. The first no-update archive stopped for a missing Development profile. Tyler then approved registration of the connected iPhone, generation/download of one matching profile, and one local archive sequence. Strict code-signature and embedded-profile checks passed on the resulting version `1.0` build `2` archive. The existing certificates, private keys, App Store profile and checked-in project signing setting were preserved; the Distribution Managed asset was not changed. The [signing record](apple-signing-reconciliation-2026-09-10.md) contains sanitized evidence and approval scope. Device installation, app-level QA, sandbox subscriptions and release acceptance retain separate gates.
- **T05–T07:** A valid signed candidate is installed on the iPhone and opens to sign-in. Native monitoring, signed-in physical-device QA, data persistence and Apple sandbox subscription transitions remain unproved. **T08** first Daily Brief/video launch scope and inputs are undecided. **T09** App Privacy answers, corrected-wordmark listing assets, truthful mailbox procedure, and read-only provider/retention evidence are incomplete; final answers and remote edits require separate gates. **T10–T11** TestFlight, review, and release are not authorized. See the [ledger](project-timeline.md) for owners, effort, dependencies, and dates.
- The production ambient-privilege preflight failed closed; the later principal-ACL inventory never ran. During its preparation a Neon owner credential was exposed locally, then reset by Tyler with containment closed. Do **not** retry the inventory or revive the operator database sequence as a launch task. See the [sanitized incident record](neon-owner-credential-containment-2026-09-15.md). The first-party operator path is conditional post-launch unless Tyler explicitly changes scope.

## Next authorized actions

1. **Primary lane T05–T07:** Build `2` is installed and the signed-out screen is verified. Tyler signs in on the iPhone with the intended QA account without sharing credentials; Codex then runs the T06 relaunch/persistence and core civic/privacy/support matrix on this candidate. Native protected-value/event and Apple sandbox purchase/account actions need their own exact gates; deletion needs a separately assigned disposable account and approval. No further device install approval is needed for the completed build `2` update. Preserve the source worktree fallback.
2. **While the account gate waits:** prepare the T06 signed-in QA steps and review the T05 native-monitoring and T07 sandbox matrix dependencies. Then complete one bounded T09 read-only public support/privacy/provider and corrected-wordmark capture evidence pass. Do not resume the retired ACL inventory or first-party operator sequence. Keep T03 server processing off and check Apple's official issue at the September 25 decision checkpoint; the September 16 watch found no fix.
3. After signed/device and T03 acceptance evidence, prepare T08 scope and T10–T11 delivery in ledger order. Obtain separate approvals for TestFlight distribution, App Review submission and release.

The October 30 target is unchanged but low-confidence/materially at risk. Keep October 2–6 free of required Tyler approvals/device/upload/submission work and preserve October 20–29 review/rework. This handoff does not replace the full ledger or the exact Standing Rules.
