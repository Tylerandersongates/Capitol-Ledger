# CapitolWonk EOD Handoff — September 10, 2026

Finalized for September 11 resumption after the September 3 documentation checkpoint was restored, the September 4–10 gap was reconciled, the exact wordmark correction was released, T03 closed for dependency candidate `f4f04de`, T04's current signing state was reconciled read-only with an approved Apple Support follow-up, and the App Privacy correction plus immediate account-deletion source were prepared while Support remains pending. Post-restart integrity, complete tracked/untracked review, secret scanning, and the consolidated local matrix passed. Tyler then authorized the final September 10 checkpoint on `codex/logo-refresh-sept10`; payload commit `871ba4e` plus its direct strict-CI correction at `a6cb1da` form the exact reviewed non-production T08/T09 source candidate. [GitHub CI run 34557134361](https://github.com/Tylerandersongates/Capitol-Ledger/actions/runs/34557134361) passed for exact SHA `a6cb1da`, matching Vercel Preview deployment [`6mKbaWGJ2jdRDbNRaB1K8ZMYKh8L`](https://vercel.com/capitolwonkce/project-qosv1/6mKbaWGJ2jdRDbNRaB1K8ZMYKh8L) reached Ready, and authenticated smoke passed on `/brief`, `/privacy`, and `/account-deleted`. T04 remains blocked pending Support guidance. The candidate is not deployed to production; no cleanup-task secret, schedule, or monitor is configured; no live account has been deleted for QA; and the App Store questionnaire is unchanged. Read [the current timeline and task ledger](project-timeline.md), [T04 signing reconciliation](apple-signing-reconciliation-2026-09-10.md), [the App Privacy correction packet](app-store-privacy-correction-2026-09-10.md), and [the account-deletion runbook](account-deletion-runbook-2026-09-10.md) with this handoff. **The existing logo stays. Production omits `CE` from the shared wordmark cards. Launch target: October 30, 2026.**

## 1) Completed Today

- Confirmed the new worktree began clean at deployed merge `e82d7ea`; inspected commit `1f54fff` and read its complete September 3 EOD and project timeline.
- Created continuation branch `codex/logo-refresh-sept10` and safely restored the exact documentation-only checkpoint as `f37f05e`. No production code, migration or external state was changed by that cherry-pick.
- Reconciled September 4–10 honestly: repository history contains no product-work evidence for the interval. Five weekdays/seven calendar days elapsed, but availability is unknown; all unresolved T01–T11 work stayed unresolved until evidenced.
- Inspected the existing web logo and complete native iPhone/iPad icon family. A preview-only concept board was generated outside the repository before Tyler clarified the request; no artwork was replaced.
- Tyler confirmed the current logo should remain and identified the actual issue: visual wordmark cards still showed `CAPITOL WONK CE`.
- Found the cause in the shared `BrandWordmark`: `publicBrandName` already resolved to `CapitolWonk`, but the visible foil span independently hardcoded `Wonk CE`. CSS, environment configuration and icon assets were not the cause.
- Committed the narrow fix as `6ce9d5b`: the shared wordmark now renders `CapitolWonk`, and `scripts/check-public-brand.ts` directly guards against restoring the split-node `CE` suffix.
- Verified the corrected local dashboard at `http://127.0.0.1:3024/dashboard` in the mobile browser viewport. The retained logo is unchanged and the card visibly reads `CAPITOLWONK` without `CE`.
- Pushed branch `codex/logo-refresh-sept10`, verified its Ready Vercel preview, then obtained Tyler's explicit approval for the production release. Fast-forwarded the exact three-commit branch to `main` at `7ec68bc`; no pull-request merge commit was added.
- GitHub CI [run 34503725976](https://github.com/Tylerandersongates/Capitol-Ledger/actions/runs/34503725976) passed in 1m 50s. Vercel production [deployment 3DA1NsWFjwPYpFgx1GhhkjGUYMNy](https://vercel.com/capitolwonkce/project-qosv1/3DA1NsWFjwPYpFgx1GhhkjGUYMNy) reached Ready/Current from exact source `7ec68bc` in 1m 45s.
- Verified the live production dashboard, sign-in, onboarding, map and Daily Brief surfaces. Each resolves to CapitolWonk without the retired `CE` suffix; the original logo remains intact.
- Found that the current T02 blocker was based on the July 29 handoff even though pushed evidence branch `codex/sentry-geo-deep-scrub-verification` contains superseding remediation evidence. Commits `46efc95` and `d7980aa` record Tyler's approved `$user.geo.**` rule in both Sentry projects, one authorized Edge event with neither raw IP nor displayed geography, and full temporary-probe cleanup. Reconciled T02 as complete for new events without running another probe or changing external configuration.
- Reconciled the same superseding handoff's historical T04–T06 evidence as the baseline for the current pass recorded below. The July 31 evidence showed a signing hard stop pending Apple Support: Xcode could then see the prepared physical device, but signed attempts stopped at provisioning, zero usable identities were found, the replacement Development certificate had no matching private key and certificate ownership was unresolved. Protected native Sentry setting resolution and an unsigned Release build had passed through a temporary mode-0600 settings file that was removed with temporary artifacts; no native event was sent, and the approved one-event scope remains unconsumed pending a valid signed/device candidate plus exact-action review.
- Updated the timeline with the missed first checkpoint, revised planning ranges, owners, dependencies, evidence and risks. The plan is behind its first internal checkpoint; October 30 remains fixed but is low-confidence/materially at risk until signing, device and subscription gates are evidenced. T03 no longer contributes an unresolved gate for exact candidate `f4f04de`.
- Completed T03's baseline registry-backed audits with exact Node `22.22.3` and pnpm `9.15.9` before changing packages. The production graph reported **2 critical, 11 high and 2 moderate** advisories across 265 dependencies; the full graph added three development-only package-version findings but only two new advisory IDs. Both criticals affected direct `next@15.5.22` and were patched from `15.5.24`. This failed baseline triggered the subsequently approved remediation recorded below; July's accepted subset remained insufficient.
- Tyler approved the narrow T03 remediation. Aligned `next`, `eslint-config-next` and `@next/swc-wasm-nodejs` on `15.5.25`, then used a targeted lock refresh that moves every compatible vulnerable transitive with the same Node/pnpm pair. Normal resolution also advances compatible packages inside the affected Next/ESLint/Sharp/Sentry build subtrees; the direct manifest changes only the three aligned Next pins. The candidate lockfile is `92c9932f6145ab3e1e4875de02ecc802112a24e8b3bc98ccffdfc213d7d28de5`; no override, unrelated direct dependency, Sentry update or package-age bypass was used.
- Re-audited the installed candidate. Production and full graphs both improve to **0 critical, 2 high and 2 moderate**. Every remaining finding is in Next's exact nested `postcss@8.4.31`; the current two-high/two-moderate set is not identical to July's accepted subset, so it required a new explicit decision.
- Completed local release-grade verification: frozen install; TypeScript, lint, brand and feedback checks; native and explicitly asserted-WASM optimized builds with 51 static pages; all 38 application/readiness validations; 19/19 optimized-server route checks; and 2/2 Next Image optimizer requests.
- Pushed exact candidate `f4f04de` to `codex/logo-refresh-sept10`. [GitHub Actions run 34518247389](https://github.com/Tylerandersongates/Capitol-Ledger/actions/runs/34518247389) passed every step in approximately 1m55s. Matching Vercel Preview deployment [`mep97jz9t5Hncx1t2mRBeTytDqut`](https://vercel.com/capitolwonkce/project-qosv1/mep97jz9t5Hncx1t2mRBeTytDqut) is Ready/Latest from the exact SHA, with no failed build stage or surfaced warning/error.
- Vercel Deployment Protection correctly intercepted anonymous bulk smoke. The existing authenticated project-owner browser session reached 14 preview app pages/redirects without stale `CE` branding and verified rendered local-logo plus allowlisted Congress.gov image-optimizer paths.
- Tyler explicitly accepted the exact candidate-specific `postcss@8.4.31` residual—`GHSA-6g55-p6wh-862q` and `GHSA-r28c-9q8g-f849` (high), plus `GHSA-qx2v-qp2m-jg93` and `GHSA-fxqj-rqcc-2cmp` (moderate). T03 is closed for `f4f04de`; the exception does not carry to a changed graph and authorizes no merge, production deployment, signing, upload or distribution.
- Completed T04's current read-only reconciliation. macOS reports zero usable code-signing identities. One non-expired Xcode-managed App Store provisioning profile matches the project, but no usable certificate/private-key identity exists. Stable Xcode 26.6 remains selected; Xcode 27 beta remains installed but unselected.
- Confirmed the paired iPhone record and Developer Mode remain known, but the device is disconnected and the available-device query returns zero physical mobile devices. No pairing, trust, Developer Mode, runtime or device setting changed.
- Located the existing open Apple Developer Support provisioning case. Tyler confirmed no reply, supplied the private case ID and explicitly approved sending the reviewed correction on that case. Apple displayed its receipt confirmation. The case ID and all protected Apple identifiers remain excluded from repository documentation.
- Preserved the certificate/CSR/Keychain/profile/signing freeze. No signed build/archive, native event, upload, distribution or review action occurred. T04 remains blocked pending Apple guidance.
- Quarantined an unexplained uncommitted `pnpm-lock.yaml` diff that appeared after the worktree and remote matched at `2443c08`. It downgraded several T03-remediated transitives without changing `package.json`. Tyler explicitly approved restoring only that lockfile to the committed T03 version and pushing the T04 documentation checkpoint. The lockfile now matches the accepted graph; no dependency change was staged, committed or pushed.
- Used the Apple wait window for source-backed App Privacy preparation. Audited the current questionnaire snapshot against account, location, official-message, StoreKit, feedback, web/native Sentry and future-video paths. Prepared `docs/app-store-privacy-correction-2026-09-10.md` with a provisional 14-type matrix: retain nine types and add Emails or Text Messages, Device ID, Crash Data, Performance Data, and Other Diagnostic Data. The resolved native SDK's persistent installation identifiers require linked diagnostic answers unless removed and verified absent; tracking remains No for the current no-video/no-analytics configuration.
- Updated the public privacy-page source to explain on-device district matching, automatic crash/app-hang/watchdog diagnostics, voluntary feedback linkage, official-contact messages, retention/deletion and conditional YouTube playback. Added readiness assertions and aligned the setup packet, release checklist and current planning docs. These changes are captured in the reviewed non-production branch checkpoint; no production deployment or App Store Connect change occurred.
- Replaced the request-only deletion source locally with a confirmed in-app permanent-delete action. One serializable transaction snapshots deduplicated `AccountDeletionCleanupJob` records for surviving Team-member pause restoration and any legacy Stripe subscription, erases the account plus linked relational/raw-store data, verifies zero linked rows, and deidentifies the completion audit. The deletion operation performs no Team-member/provider mutation before its account commit: a database failure rolls back jobs and deletion, preserving the owner. After commit, cleanup runs idempotently inline and through `/api/tasks/account-deletion-cleanup`; jobs remain only while pending/retrying and are erased after success. Missing/terminal Stripe results complete with checkout-required or already-complete semantics, while transient failures retry. For an active legacy Stripe subscription, cleanup schedules renewal off and detaches account metadata where Stripe permits; Apple billing remains separate.
- Hardened stale and concurrent activity around deletion. Production account lookup is exact-ID and read-only, with no missing-user creation, email remap, or process-memory fallback. Signed Stripe webhooks enforce a five-minute timestamp tolerance, reconcile live provider state, reject stale subscription IDs, and, before acknowledging a deleted-user event, schedule an active subscription not to renew and detach metadata where Stripe permits; terminal/missing subscriptions count as complete. Team-seat pause writes lock the account/workspace against deletion and attempt Stripe resume compensation if their database transaction fails. Rate-limit subjects are hashed/expired and cleared for the deleted account. The client uses an explicit success receipt, labels ambiguous results unconfirmed, and sets a multi-tab fence honored by browser data writers and native StoreKit publication.
- Added the deletion-integrity, cleanup-outbox, and Team-pause workspace-integrity migrations plus deletion fixture/readiness coverage and documented the remaining operational gates. The pause migration converts only the known empty and owner-upgrade sentinels to `NULL`, aborts on an unexpected orphan, then enforces the nullable real-workspace cascade. No production migration/deploy, task-secret/scheduler/monitor configuration, destructive live QA, or provider-cleanup QA occurred. Release still requires live schema/FK verification, a protected cleanup task and age/retry monitor, disposable production-shaped account deletion, real-Postgres concurrency, Stripe webhook/cleanup/compensation evidence, physical-device multi-tab/fence QA, backup/PITR tombstone policy, and retention/removal procedures for Apple, Stripe, Sentry, email providers and official-message recipients.
- Recovered cleanly from the session memory restart. Read-only checks found no empty or truncated source, merge/reject/swap artifacts, unresolved conflict markers, or missing final newlines. At that checkpoint, branch `codex/logo-refresh-sept10` and its remote both resolved to `2e4e04b`; `pnpm-lock.yaml` still matched the accepted T03 graph. Tyler subsequently authorized the reviewed final branch checkpoint recorded below.
- Verified the public CapitolWonk YouTube channel in the authenticated browser at `https://www.youtube.com/@CapitolWonk`, then configured that canonical URL in the reviewed branch candidate with `episodes` still empty. The channel-only state exposes the real Subscribe link while retaining “First video coming soon” and loading no iframe/player. Updated the public privacy wording and publishing guide for user-initiated outbound YouTube navigation. No video, embed, captions, transcript/source review, production deployment, or launch-scope decision exists yet.
- Final independent pre-push review corrected the unconfirmed deletion page so direct navigation does not claim browser cleanup occurred, and hardened legacy Stripe customer cleanup to traverse every subscription page. Initial missing-customer results complete safely; a malformed cursor or later-page provider error now fails closed so the cleanup job retries instead of silently dropping previously discovered subscriptions. Multi-page and later-page error fixtures pass.
- Committed the reviewed privacy/deletion and channel-only YouTube payload as `871ba4e`. Its first exact-head CI run exposed one strict unused-parameter binding; the source correction in `a6cb1da` removed only that binding while preserving the accepted input shape, and its accompanying documentation edits reconciled the checkpoint wording. Strict local TypeScript and the targeted readiness checks passed, the corrective six-file delta passed independent review, and the branch synchronized with its upstream at `a6cb1da`.
- Verified the exact corrected source candidate remotely. [GitHub CI run 34557134361](https://github.com/Tylerandersongates/Capitol-Ledger/actions/runs/34557134361) passed in 1m 50s; its only annotation is the non-blocking GitHub Actions Node 20 deprecation warning. Matching Vercel Preview deployment [`6mKbaWGJ2jdRDbNRaB1K8ZMYKh8L`](https://vercel.com/capitolwonkce/project-qosv1/6mKbaWGJ2jdRDbNRaB1K8ZMYKh8L) reached Ready. Authenticated preview smoke verified `/brief` shows “First video coming soon,” the exact Subscribe target, and no player; `/privacy` renders the correction; and direct `/account-deleted` navigation remains explicitly unconfirmed.

## 2) Current State

**Verified candidate evidence:** the shared wordmark correction reaches dashboard, onboarding, sign-in, map and the desktop site header. Remote dependency candidate `f4f04de` passes its frozen install, native and true-WASM production builds, TypeScript/lint, complete 38-check application/readiness suite, 19/19 HTTP routes and 2/2 image-optimizer requests. The unexplained lockfile downgrade never entered candidate evidence and was restored to the committed T03 version with Tyler's explicit approval. The privacy correction, account-deletion implementation, and channel-only YouTube integration are captured in exact non-production source candidate `a6cb1da`; its final exact-head CI, matching Ready Preview, and targeted preview route smoke pass. Production/runtime gates remain open below.

**Checkpoint handoff boundary:** before commit, the privacy/deletion package plus channel-only YouTube update was reviewed as one complete 86-tracked/14-untracked change set. Independent tracked and untracked audits found no unrelated file, secret, private Apple case ID, lockfile drift, or staging blocker; local validation passed. Tyler authorized the final checkpoint on the existing `codex/logo-refresh-sept10` branch. Payload `871ba4e` and direct strict-CI correction `a6cb1da` are pushed; the branch evidence gate is complete for exact source candidate `a6cb1da`. The push did not change `main` or production and did not authorize migrations, configuration, destructive/provider QA, App Store Connect edits, upload, distribution, or release. The next independent gate is the read-only production migration-history preflight and ordered approval packet.

**Production baseline:** `7ec68bc` is the verified live release on `main`; CI and the Ready/Current Vercel deployment passed, and five-route live smoke confirmed the CE-free shared wordmark. The updated privacy page, immediate deletion/cleanup-worker path, three supporting migrations, and channel-only YouTube link are not in production; no cleanup-task secret/schedule/monitor exists; App Store Connect still has its prior nine-type questionnaire. Beyond that verified web release and the approved Support follow-up, no production configuration, database, Apple signing/security, distribution, native upload or review state changed. The public channel is verified, but its app connection remains branch-only and has no episode/player. The daily-editions migration is already applied and must not be repeated.

**Schedule:** T01–T03 are complete for their exact evidence/candidate scopes, but the September 4 checkpoint slipped. Exact T08/T09 source candidate `a6cb1da` now passes branch CI, Ready Preview, and targeted preview smoke. T04's read-only pass is complete and the corrected Support follow-up is submitted; signing remains blocked with zero usable identities while Apple response time is external. Approximately **8–15 working days** remain across T04–T11, excluding additional QA fixes, re-review and external Apple/provider waits. Completed privacy preparation and branch evidence narrow T09's remaining preflight/documentation slice to roughly 0.25–0.5 day but do not move the overall rounded range. Retain September 25 as a readiness decision, with actual readiness more plausibly September 29–October 1 only if T04 closes without additional delay. Preserve October 2–6 as zero owner-dependent capacity, October 7+ as tentative, October 16 as the conditional final-candidate checkpoint, October 19 as the approval-gated proposed submission target, October 20–29 for review/rework and October 30 for final go/no-go.

| ID | Status, owner, evidence, estimate and risk |
| --- | --- |
| T01 | Completed and deployed September 10. Tyler retained the logo; `6ce9d5b` implemented the wordmark fix, and production source `7ec68bc`, CI, Vercel and live smoke pass. No T01 hands-on effort remains; current web-card risk is low/closed. The two listing screenshots still contain `CE` and must be recaptured before reuse under T09. |
| T02 | Completed for new events; superseding evidence recovered September 10. Tyler chose remediation. Commits `46efc95` and `d7980aa` record the recursive `$user.geo.**` rule in both projects, a single authorized July Preview Edge event with no raw IP or displayed geography, and complete probe cleanup. No T02 effort remains; the release gate is closed for new events based on that historical one-event verification. Old events were not rewritten, no September probe or protected change was made, and the live rule was not re-read because the available Sentry session is signed out. |
| T03 | Completed for exact candidate `f4f04de`. The aligned Next `15.5.25` candidate clears both criticals and every compatible transitive high; production/full audits are 0 critical, 2 high and 2 moderate, all in Next's exact nested `postcss@8.4.31`. Local install/build/regression, unsigned iOS Simulator Release, exact-head CI and matching Ready Vercel Preview pass. Tyler explicitly accepted the exact four-advisory residual. No T03 effort remains unless the dependency graph changes; accepted/monitored risk. Keep the exception visible and move to a supported patch when available. |
| T04 | Active and blocked pending Apple Support. Current read-only checks report zero usable signing identities. One non-expired Xcode-managed App Store profile matches the project, but no usable certificate/private-key identity exists. The corrected follow-up on the existing case was explicitly approved, submitted and acknowledged. Apple Support owns guidance; Tyler owns the next exact approval; Codex owns the approved path/build. 1–2 days after guidance/approval plus unknown wait; high risk. Preserve the certificate/CSR/Keychain/profile/signing freeze. |
| T05 | Protected/native monitoring checks partial. Protected native-setting resolution and an unsigned Release build passed through a temporary mode-0600 file that was removed with temporary artifacts. No native event was sent; the approved one-event scope remains unconsumed and paused until a valid signed/device candidate plus exact-action review. Codex owns checks; Tyler owns that review. 1–2 days after T04; high risk. No outbound Brief sends or scheduler activation. |
| T06 | Physical-device QA pending. The paired iPhone record and Developer Mode remain known, but the current query reports it disconnected and zero physical mobile devices available. No verified signed candidate installed and app-level QA has not begun. Tyler owns device/session; Codex owns matrix/fixes. 2–3 days initial QA plus fixes/retest; high risk. |
| T07 | Subscription baseline/transitions pending. Tyler owns account/action approval; Codex owns validation; Apple timing is external. 1–2 days after candidate plus propagation wait; high risk. Do not repurchase. |
| T08 | Public channel created and verified at `https://www.youtube.com/@CapitolWonk`; its canonical URL is configured in exact non-production source candidate `a6cb1da` with no episodes. Authenticated HTTPS Preview smoke retains the honest placeholder, shows the real Subscribe target, and loads no iframe. First-video content, embed/captions/transcript/sources, production or physical-iPhone QA, real-player runtime privacy evidence, and launch-scope decision remain open. Tyler owns content/access/separate approval; Codex owns integration/QA. 1–2 days after video inputs plus platform wait; medium/high risk. Confirm launch scope rather than silently making it a blocker. |
| T09 | Privacy source candidate prepared and branch-verified; remaining release assets/evidence await T04–T07 and T08 scope. Exact candidate `a6cb1da` contains the reviewed provisional 14-type correction packet, public-policy source, atomic account-deletion/cleanup-outbox path, three new supporting migrations, task route, and runbook. Exact-head CI, matching Ready Vercel Preview, and targeted `/brief`, `/privacy`, and `/account-deleted` preview smoke pass. This is not production deployment, migration, task/provider configuration, account-deletion QA, App Store publication, or device proof. Production proof remains open: exact archive/runtime/WKWebView/providers; production migration-history inspection and conditional deployment of the older deletion-request migration before the three new migrations; live tables/FKs and sentinel/orphan handling; cleanup-task secret, schedule, no-payload age monitor, retry/reclaim behavior; exact-ID/no-memory-fallback persistence; disposable-account deletion; webhook timestamp/live-state/deleted-user handling; post-commit Team restoration and legacy Stripe terminal/transient cleanup; real-Postgres pause/delete serialization and compensation; completed-job erasure with pending-only retention; explicit receipt/ambiguous-result and physical-device multi-tab-fence behavior; deletion-transaction rollback with no provider mutation; backup/PITR tombstones; provider procedures; App Store publication; and screenshot recapture. Codex owns the read-only production migration-history preflight, reconciliation/recapture, and approval packet; Tyler owns each exact remote approval. Estimate: 0.25–0.5 working day for remaining preflight/documentation preparation only; production/runtime/device work, fixes, re-review and external waits are additional. Risk: high. Current listing screenshots still show `CE`. |
| T10 | Upload, tester distribution and review pending; none occurred for this candidate. Tyler owns each exact approval; Codex owns preparation; Apple is external. 0.5–1 day hands-on plus unknown processing/review; high risk. |
| T11 | October 30 target only, not release authorization. Tyler owns final go/no-go; Codex owns evidence/options. 0.25–0.5 day decision prep, external waits excluded; high risk until T04–T10 clear. |

Deferred tracks remain unchanged: provider-backed rate limiting; final auth-email delivery/volume; civic-data freshness/fallback labeling; source-grounded AI/live-provider verification; Senate vote/additional sync scope; post-launch Daily Brief email/push and authenticated publishing automation; Supreme Court sister app only after main-app TestFlight; state legislation later in the main app, tentatively early 2027; historical Round 1–3 exports remain non-distributable.

## 3) Environment And Config Changes

- Targets touched across the full September 10 session: local branch/worktree, GitHub branch and `main`, Vercel preview and existing Vercel production. T03 itself changed only the local/GitHub review branch and its automatic Vercel Preview; `main` and production remain at the previously verified wordmark release `7ec68bc`.
- Local preview uses `AUTH_DEMO_ENABLED=true`, `DAILY_BRIEF_LAYOUT_PREVIEW=true` and port `3024` for this session. These are process-only overrides; no environment file or protected value changed.
- The source worktree's ignored `node_modules` link still points at the earlier installed graph and was not mutated. Resolution, install and all candidate checks ran in isolated `/private/tmp/capitolwonk-t03.y13zNy` with a real pnpm 9 store; only the reviewed manifest and byte-identical generated lockfile were then applied to this branch.
- No new production Vercel configuration/deployment, Neon migration/write/deletion, account-deletion cleanup task secret/schedule/monitor, Sentry configuration, Apple signing/security, credential, Keychain, native upload, tester distribution, App Store questionnaire or review change was made during privacy/deletion preparation. The public YouTube channel was verified read-only and its canonical URL was added only to the non-production branch candidate; no YouTube setting, video, provider integration or production deployment was changed by Codex. Earlier external mutations remain limited to the approved Git pushes/resulting Vercel deployments and the explicitly approved sanitized follow-up on the existing Apple Developer Support case.

## 4) Verification Run

- Public brand check through the installed Node 22/tsx runtime: passed; the new direct wordmark assertion passed.
- `node scripts/check-launch-copy-tone.mjs`: passed.
- `node scripts/check-testflight-mobile-ui.mjs`: passed.
- `node scripts/check-ios-native-bridge.mjs`: passed; stable native/purchase identities remain intact.
- `tsc --noEmit --pretty false --noUnusedLocals --noUnusedParameters`: passed.
- `git diff --check`: passed for the documentation closeout.
- Mobile browser QA at `http://127.0.0.1:3024/dashboard`: passed; retained logo plus visible `CAPITOLWONK`, no `CE`.
- Local HTTP rendering checks for `/dashboard`, `/sign-in`, `/onboarding`, `/map` and `/brief`: passed; each rendered the shared CapitolWonk wordmark with no stale `CE` suffix.
- GitHub CI run `34503725976`: passed; Quality checks completed with no failing job. A non-blocking GitHub warning says several `@v4` actions still target deprecated Node.js 20 and were forced onto Node.js 24.
- Vercel production deployment `3DA1NsWFjwPYpFgx1GhhkjGUYMNy`: Ready/Current from exact source `7ec68bc`; production alias is `https://project-qosv1.vercel.app/`.
- Live production browser smoke on `/dashboard`, `/sign-in`, `/onboarding`, `/map` and `/brief`: passed; CapitolWonk appears without `CE`, and the existing logo remains unchanged.
- Supported dependency toolchain: Node `22.22.3` plus pnpm `9.15.9`, matching the declared package manager and CI major/version.
- `pnpm audit --prod --json`: completed after a sandboxed `ENOTFOUND registry.npmjs.org` and network-enabled retry; exited `1` with 2 critical, 11 high and 2 moderate advisories across 265 production dependencies.
- `pnpm audit --json`: completed; 18 package-version findings—2 critical, 14 high and 2 moderate—across 619 total dependencies, representing 17 distinct advisory IDs. The extra three findings are development-only ESLint-chain highs; one repeats the production `brace-expansion` advisory against a second locked version.
- Approved remediation: exact direct pins for `next`, `eslint-config-next` and `@next/swc-wasm-nodejs` moved from `15.5.22` to `15.5.25`; compatible transitives resolve to patched Sharp, fast-uri, brace-expansion, nanoid and js-yaml versions. `pnpm-workspace.yaml`, Sentry and every other direct dependency remain unchanged; no override or package-age bypass occurred.
- Candidate frozen install: passed with Node `22.22.3`/pnpm `9.15.9` and a dedicated real store. No minimum-age setting or bypass was present, but this does not certify the missing historical external guard; the incidentally resolved `@typescript-eslint@8.70.0` family was about two days old. The install's dependency scripts also confirm the workspace `allowBuilds` field is not enforced by this pnpm 9 surface. Treat both controls as separate tooling follow-ups.
- Candidate audits: production and full graphs both report 0 critical, 2 high and 2 moderate findings. All four are the exact `postcss@8.4.31` copy nested by Next 15.5.25.
- Candidate static verification: TypeScript, ESLint, brand and feedback readiness pass. The native SWC build and an explicitly asserted WASM SWC build both pass with 51 generated static pages; a direct binding probe and transform confirmed the latter used `@next/swc-wasm-nodejs@15.5.25`.
- Candidate application/readiness suite: 38/38 validations pass. Expected warnings only reflect intentionally absent demo-safe external configuration; no outbound provider request or database connection was made.
- Candidate optimized-server smoke: Next 15.5.25 became ready on loopback in 710 ms; 19/19 historical endpoints returned their expected 200/307/401/405 statuses with no 5xx, and 2/2 local/allowlisted-remote image optimizer requests returned nonempty images. The server stopped cleanly.
- Candidate unsigned native regression: Xcode 26.6 Simulator Release build passed for iOS 16.0 with arm64/x86_64 output and both signing flags disabled. This is compile/link evidence only; it does not change or clear the T04 signing/archive/device gate.
- Candidate branch CI: [run 34518247389](https://github.com/Tylerandersongates/Capitol-Ledger/actions/runs/34518247389) passed for exact SHA `f4f04de`; frozen install, preview-runtime check, TypeScript, brand, lint, feedback readiness and production build all passed.
- Candidate Vercel Preview: deployment [`mep97jz9t5Hncx1t2mRBeTytDqut`](https://vercel.com/capitolwonkce/project-qosv1/mep97jz9t5Hncx1t2mRBeTytDqut) is Ready/Latest from `f4f04de`. Authenticated browser smoke reached 14 application pages/redirects with no stale `CE` and loaded both local and Congress.gov images through Next Image. Anonymous curl was stopped at expected Deployment Protection, not an application error.
- Candidate-specific residual decision: Tyler accepted the exact Next-nested PostCSS two-high/two-moderate set after reviewing the audit and external evidence. T03 is closed only for `f4f04de`; re-audit any changed graph.
- T04 identity check: `security find-identity -p codesigning` completed normally with zero identities and zero valid identities.
- T04 provisioning check: one structurally valid, non-expired, Xcode-managed App Store profile matches the project by boolean-only comparison and embeds one certificate. CMS integrity was checked without chain-trust validation; profile presence does not clear the zero-identity blocker.
- T04 tool/device check: stable Xcode 26.6 is selected; Xcode 27 beta is installed but unselected. The paired iPhone is remembered with Developer Mode enabled but disconnected; zero physical mobile devices are currently available.
- T04 Support check: the authenticated portal shows one open provisioning case and offered same-case email follow-up. Tyler confirmed no reply and approved the exact correction plus private case ID. Apple's receipt page confirmed submission; no protected identifier was copied into docs.
- Workspace-integrity check: dependency pins and the lock graph remained unchanged. `package.json` has intentional branch-candidate script changes for expanded account-deletion and persistence checks. The unexplained lockfile downgrade was quarantined, excluded from candidate evidence and restored with Tyler's explicit approval; `pnpm-lock.yaml` matches the committed T03 version.
- App Privacy static verification: direct Node runs of feedback readiness (19 pass, 6 expected non-production configuration warnings), account-deletion readiness, launch-copy tone, TestFlight prep (93 pass, 5 expected protected-configuration warnings), TypeScript and targeted ESLint all passed. The package-manager wrapper was not used after it detected the worktree's known older shared install and attempted a protected modules refresh; no install or dependency change occurred.
- App Privacy visual verification: the local `/privacy` page returned 200 and rendered all 15 expected headings at a 492-by-783 mobile viewport with document width equal to viewport width, no horizontal overflow and no browser warning/error. The external Google/YouTube privacy link resolves to the intended HTTPS policy URL. This visual-only check used the existing shared local install and is not exact-candidate dependency evidence.
- Independent review drove the Device ID/linkage, deletion-target, provider, retention, consent, Stripe, YouTube, and WKWebView corrections. A post-restart reconciliation also corrected an overbroad public Stripe sentence and stale secondary readiness instructions. The current local checks pass, but the packet remains provisional until its named runtime and operational gates pass.
- Account-deletion source verification covers atomic fixture success, transactional cleanup-job snapshots, raw-store cleanup, cross-workspace invitation and unrelated-user preservation, rollback without committed jobs, retry of an existing request, already-deleted idempotency, exact-ID persistence safety, Stripe timestamp/stale/deleted-user guards, terminal/missing cleanup behavior, hashed rate-limit cleanup, and browser receipt/fence assertions. Static/readiness coverage also guards the protected task route and post-commit worker contract. This remains local fixture/static evidence only; it does not prove live schema/FKs, task scheduling/age monitoring, real-Postgres pause/delete serialization, Stripe provider/compensation behavior, physical-device multi-tab/native-writer behavior, backup/PITR deletion, or a real web/TestFlight account flow.
- Post-restart EOD matrix: TypeScript, full ESLint, Prisma validation, deletion transaction/cleanup/persistence/readiness fixtures, billing transition and local-prep readiness, feedback readiness, TestFlight local-prep/mobile UI, launch copy, iOS bridge and `git diff --check` all passed. Expected warnings reflect absent protected production database, App Store and Sentry configuration; strict production-mode gates, live Postgres/Stripe behavior and physical-device TestFlight verification remain open.
- Final checkpoint verification: reviewed payload commit `871ba4e` and direct strict-CI correction `a6cb1da` are pushed. The branch and upstream matched at `a6cb1da`; strict TypeScript with unused-local/parameter enforcement, account-deletion readiness, TestFlight local-prep, launch-copy, whitespace, and private-case-ID exclusion checks passed. Independent review found no blocker in the six-file correction.
- Final checkpoint remote evidence: [GitHub CI run 34557134361](https://github.com/Tylerandersongates/Capitol-Ledger/actions/runs/34557134361) passed for exact SHA `a6cb1da`; matching Vercel Preview deployment [`6mKbaWGJ2jdRDbNRaB1K8ZMYKh8L`](https://vercel.com/capitolwonkce/project-qosv1/6mKbaWGJ2jdRDbNRaB1K8ZMYKh8L) is Ready. The only CI annotation is the non-blocking GitHub Actions Node 20 deprecation warning.
- YouTube and privacy Preview verification: the authenticated public page confirmed `CapitolWonk` and `@CapitolWonk`; the local Daily Brief guard, video/editorial/dated-edition fixtures, launch-copy, feedback readiness, mobile UI, TypeScript, targeted ESLint and diff checks passed. Local and exact-candidate HTTPS Preview `/brief` show “First video coming soon,” the exact Subscribe URL, and no player/episode. Preview `/privacy` renders the correction, and direct `/account-deleted` navigation remains explicitly unconfirmed. No YouTube setting, production deployment, privacy publication, account deletion, or provider action occurred.

## 5) September 11 Assigned Task (Completed Read-Only)

The assigned September 11 task was to inspect production migration history read-only, resolve the conditional order in the [account-deletion runbook](account-deletion-runbook-2026-09-10.md), and prepare the approval packet without executing it. That task is complete: the older deletion-request migration is applied, exactly the three September 10 migrations remain, and the [ordered packet](production-privacy-deletion-approval-packet-2026-09-11.md) preserves separate action-time gates. The Apple Support wait and T04 signing freeze remain unchanged.

The continuation began with:

```bash
git status --short --branch
git rev-list --left-right --count '@{upstream}...HEAD'
git log -1 --oneline --decorate
git show --stat --oneline --decorate HEAD
git show --check --format=fuller HEAD
git show --name-status --format= HEAD
sed -n '1,260p' docs/account-deletion-runbook-2026-09-10.md
```

Confirm the exact source candidate `a6cb1da` remains present and its recorded CI/Preview evidence still belongs to that SHA. If the only newer delta is this documentation closeout, do not repeat application CI or Preview smoke; rerun exact-head evidence after any source, dependency, migration, configuration, or test change.

The migration-history step then used the existing intended-production connection without printing `DATABASE_URL`:

```bash
/Users/tylergates/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node node_modules/prisma/build/index.js migrate status
```

### September 11 Continuation Result

The read-only production migration-history preflight is complete. `20260718154000_account_deletion_requests` is already applied; Prisma reports exactly the three September 10 migrations as pending. Aggregate-only checks found zero Official Contact, Petition, Team-pause user, or Team-pause workspace orphans; the two existing Team-pause rows reference valid workspaces and use no known sentinel. `TeamSubscriptionPause.workspaceId` remains non-nullable until the final pending migration, and `AccountDeletionCleanupJob` is absent until the middle pending migration, both as expected. The ordered migration, deployment, cleanup-task, monitoring, destructive-account, Stripe/concurrency, signed-device, and App Privacy steps are separated in the [September 11 production privacy/deletion approval packet](production-privacy-deletion-approval-packet-2026-09-11.md). No production write, deployment, protected configuration, provider mutation, account deletion, App Store change, signed build, upload, distribution, submission, or release occurred.

## 6) Resume Prompt For New Thread

> Continue CapitolWonk from `docs/eod-handoff-2026-09-10.md`, `docs/project-timeline.md`, and `docs/production-privacy-deletion-approval-packet-2026-09-11.md`, using the linked dependency, signing, privacy, and deletion documents as needed. Exact non-production source candidate `a6cb1da` passes GitHub CI run `34557134361`, matching Ready Vercel Preview `6mKbaWGJ2jdRDbNRaB1K8ZMYKh8L`, and authenticated `/brief`, `/privacy`, and `/account-deleted` smoke. Production remains `7ec68bc`. The September 11 read-only production preflight confirms `20260718154000_account_deletion_requests` is applied and only the three September 10 migrations are pending; aggregate orphan/sentinel checks are clean. Review the packet and seek separate action-time approval for each batch before any migration, deployment, protected configuration, destructive account/provider QA, App Store Connect publication, signed build, upload, tester distribution, submission, or release. T01–T03 remain complete. T04 is blocked pending Apple Support with zero usable signing identities; preserve the certificate/CSR/private-key/Keychain/profile/team/bundle/signing/device-state freeze and inspect any reply read-only before presenting one exact supported action. The private case ID is not stored in repository documentation. The YouTube channel remains configured with `episodes` empty and has no first video/player. Approximately 8–15 working days remain, excluding additional QA fixes, re-review and external Apple/provider waits; October 30 remains low-confidence/materially at risk. Carry T04–T11 and every deferred track.
