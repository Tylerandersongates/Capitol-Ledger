# CapitolWonk — Current Timeline and Task Ledger

## September 18 full-app launch-readiness audit

The [full-app audit](full-app-audit-2026-09-18.md) reviewed Production behavior, source, official-data truth, auth/accounts, privacy/deletion, billing, civic workflows, native behavior, performance, accessibility, scheduled operations, security, tests, and T01–T11 release controls. **Current result: not ready for TestFlight, App Review, or a public launch candidate.** The highest current defect is a false Production Alerts relationship: the newest Senate nomination vote has no bill, but Alerts falls back to unrelated H.R. 9954, calls the combination a current vote reminder, and directs the user to contact a Senator. Remove the fallback and suppress unverified reminders before the tracker work. In-app account deletion remains disabled and is a separate P0 App Review gate until its production/device runbook passes. P1 findings include the stale render-time-labeled Live Docket, sponsors missing from all 54 inspected docket rows, gamification window/streak/progress/badge defects, an unreachable Officials score, slow oversized Search, an abandoned-draft official-contact cooldown, missing browser headers, instance-local rate limiting, personalized cache-policy gaps, public proxy amplification, accessibility failures, and open T03/T05/T07/T09 evidence. Thirty-four focused checks passed, but no fresh full type/lint/build claim is made because the local runtime did not match the pinned toolchain and strict TypeScript stalled. No Production or protected state changed. **Superseding weekend order:** Saturday completes Alerts P0, truthful tracker/sponsor/sync candidate, and the gamification contract; Sunday completes gamification/account scenarios, contact repair, baseline security/accessibility work, and matching Preview/device QA. Monday starts with any failed regression, then Officials evidence persistence and Search pagination. The earlier lead is withdrawn until these candidates pass; September 25 remains the verifier checkpoint and October 30 remains possible only at low confidence.

## September 18 T06 Official Public Record Accountability audit

Production review of Bryan Steil, Lloyd Doggett, and Ted Cruz plus source tracing found that the evidence-first safeguard is working: all three profiles keep missing disclosure/ethics data visible and withhold an overall score. The score cannot currently be produced for any official, however, because voting participation is the only one of four factors with a numeric value while eligibility requires three scored factors covering 60%. The displayed records are also capped at 20 recent votes and 12 sponsored plus 12 cosponsored bills, with no date window or cap disclosure. Request-time live enrichment has a 2.5-second page budget, shorter than its four-second legislation and ten-second vote timeouts, so stored fallback and later cached results can produce different evidence totals. Lloyd Doggett showed zero legislation despite an official 119th Congress record identifying legislation he introduced. Identity URLs are counted as limited transparency sources, issue matches are undrilled keyword signals, and v1.0 has no inspectable public methodology or source-sync date. See the [audit](official-accountability-snapshot-audit-2026-09-18.md). Preserve the weekend tracker/gamification order; use truthful scope/freshness copy as a stretch goal, move deterministic evidence coverage to Monday, and keep numeric official scores disabled pending real disclosure/ethics sources and reviewed methodology.

# September 18 T06 Dashboard Civic Activity and gamification audit

Live signed-in Production and source inspection found that the visible 440-point Level 3 score reconciles to the account's stored events, but several Dashboard/Impact/Badges claims are not backed by the data model. `monthlyGain` is a cumulative integer with no month key or reset, while lifetime event counts feed charts labeled “This month.” “Day streak” increments on any new activity date without checking for consecutive days, and the week graphic invents a contiguous run from the count. Level 3 progress uses `440/750 = 59%` instead of the roughly 11% earned inside the 400–750 tier. “Civic Activity 4” includes only bills, votes/elections, letters and comments even though 14 stored events produce the 440 score. Nine of 28 displayed badges have no earning path; Civic Streak is wired to alerts read and Change Maker to a single-category threshold rather than their descriptions. Dedupe identity remains device-local while the server accepts whole client-authored aggregate snapshots, leaving multi-device replay and stale-write risks. See the [audit](dashboard-civic-activity-gamification-audit-2026-09-18.md). **Weekend order:** Live Bill Tracker freshness remains Saturday's first core repair; gamification metric truth/formula/badge work follows Saturday, with persistence, Preview and account/device scenarios Sunday. No Production action is authorized by this audit.

## September 18 T06 Live Bill Tracker freshness audit

Production inspection and source tracing found that the Dashboard/Live Docket is not yet a continuously updated live tracker. It reads the 50 bills with the newest stored action dates plus vote-linked bills from Neon, caches the database read for 60 seconds, and derives status/counts from each row's single stored latest-action string. The “Updated” date is `generatedAt`, the render time, rather than a source-sync timestamp. Production showed 54 records under “Today” and “Updated Sep 19”: 50 latest actions dated July 27 and four dated July 23; H.R. 7008's August 6 action was absent. Bill Details separately refreshes official actions from Congress.gov in memory, so a detail page can be current while the docket stays stale. `pnpm sync:congress` is an explicit operator-run write path; the repository has no Congress sync task/`vercel.json` cron declaration, and Vercel Project Settings showed no configured cron job. The saved tracker also depends on its saved bill appearing inside the bounded docket result and derives an eight-step/percent display from latest-action keywords. See the [audit](live-bill-tracker-audit-2026-09-18.md). **Weekend recovery:** Saturday prepares truthful freshness copy, persisted sync metadata, a protected bounded sync task, refreshed docket/saved-bill queries, and focused Preview evidence before continuing phone QA. Sunday closes Preview/failure behavior, continues the matrix, and presents any exact Production action separately. This newly found core defect consumes the provisional three-to-four-day lead; September 25 remains the fixed checkpoint.

## September 18 T06 member-return Production release

Tyler approved [PR #48](https://github.com/Tylerandersongates/Capitol-Ledger/pull/48) at exact source head `aea0c827829a8b4c0fda974a40b84d125455958e` after its focused local checks, 3/3 [exact-head CI](https://github.com/Tylerandersongates/Capitol-Ledger/actions/runs/35408159347), Ready [Preview](https://vercel.com/capitolwonkce/project-qosv1/DmViwxFBderQJ6F9YEuBQnjXgjyF), and Preview route pass. GitHub merged it as [`3cb37b4`](https://github.com/Tylerandersongates/Capitol-Ledger/commit/3cb37b43f74ff96108048a0148dd84d5c276affb). Vercel automatically built [Production deployment `HaDLyFG9x`](https://vercel.com/capitolwonkce/project-qosv1/HaDLyFG9x64VT7fYUG3vjjLCHeMJ), which is **Ready/Latest** for that exact merge with `www.capitolwonk.com` assigned. A signed-in live browser loaded H.R. 7008 Overview, confirmed sponsor **Rep. Bryan Steil**, opened `/members/S001213`, confirmed the Bryan Steil profile and `Go back` control, and returned to the exact H.R. 7008 Overview URL. Because the shared member route uses the history-aware control, this repair applies to sponsor and cosponsor profiles across bills, with Dashboard as the direct-entry fallback. Tyler then confirmed on the installed iPhone that back taps all work correctly and bill Details are correct, closing this focused route/content retest. Tyler committed full workdays on September 19 and 20. Saturday now targets Dashboard/Home, Account/Settings, and Search/Bills/Officials/Votes; Sunday targets civic, Alerts, Daily Brief placeholder, Privacy/Support, defect retest, and a consolidated T06 result. If clean, T05/T09 preparation starts Sunday. This creates a potential three-to-four-day working lead by Sunday evening, while September 25 and later gates stay fixed until evidence supports a forecast update. No TestFlight, App Store, provider, database, or privacy configuration changed.

## September 18 T06 phone observation and sponsor defect

Tyler first reported H.R. 7008 opening in **2.60 seconds** on the installed iPhone, versus the earlier **6.49-second** baseline (3.89 seconds, about 60% lower). Tyler found Sponsor missing although the official record names Bryan Steil. The shared bill-detail path skipped secondary official metadata for Overview when the stored record lacked sponsor data. Tyler approved [PR #47](https://github.com/Tylerandersongates/Capitol-Ledger/pull/47) at exact source head `eb2bd46`; local bill-detail, Bill Basics, action-log, syntax and diff checks passed, as did [exact-head CI](https://github.com/Tylerandersongates/Capitol-Ledger/actions/runs/35406542726) and the matching [Preview](https://vercel.com/capitolwonkce/project-qosv1/DFz594pn2BbiWMaZeqJ9LReVv72V). GitHub merged it as [`baa0fbd`](https://github.com/Tylerandersongates/Capitol-Ledger/commit/baa0fbd604ce7dcf8b37d7bf65c2405a1fb27e36). The automatic [Production deployment](https://vercel.com/capitolwonkce/project-qosv1/2k4sZxUV7zg6hKbVN3SZZdZfXbxP) is Ready with `www.capitolwonk.com` assigned. Live Overview first shows the streamed “Checking official sponsor” state, then **Rep. Bryan Steil** with a member link. Signed-in live Details updated Official sources from five cards to six and includes the sponsor profile; the current Summary and action dates remain intact. Tyler then reported two additional installed-iPhone openings at **1.75** and **1.66 seconds**. Their **1.705-second mean** is 0.895 seconds, about 34%, below 2.60 seconds and about 74% below 6.49 seconds. Comparable start/stop and network conditions were not independently reconfirmed, so these are promising observations rather than a complete latency pass. Tyler confirmed **Bryan Steil** appears on the installed iPhone and that the link opens his profile. The profile back arrow then returned to Dashboard because it was a fixed Dashboard link. Ready [PR #48](https://github.com/Tylerandersongates/Capitol-Ledger/pull/48) at `aea0c82` uses the shared history-aware back control so a bill-originated profile returns to the exact bill/tab, while direct entry retains the Dashboard fallback. Focused member-profile and syntax checks passed, [exact-head CI](https://github.com/Tylerandersongates/Capitol-Ledger/actions/runs/35408159347) passed 3/3, and the matching [Ready Preview](https://vercel.com/capitolwonkce/project-qosv1/DmViwxFBderQJ6F9YEuBQnjXgjyF) completed the H.R. 7008 → Bryan Steil → same H.R. 7008 Overview return route. A separate Production gate and the paid source-card/layout check remain. Preserve the ordered T06 matrix after this defect.

## September 18 later continuation — PR #46 merged and live

- **T06 current bill text and timeline release:** Tyler approved the reviewed [PR #46](https://github.com/Tylerandersongates/Capitol-Ledger/pull/46) merge at source head `2247fe0`. GitHub showed 3/3 checks passed and no conflicts; Codex marked the draft ready and merged it through the normal merge-commit flow as [`a2d77d1`](https://github.com/Tylerandersongates/Capitol-Ledger/commit/a2d77d16e4a0cc1a0a30cdadc950d66d0997e978). Vercel automatically built [Production deployment `58PhEQx`](https://vercel.com/capitolwonkce/project-qosv1/58PhEQxCbmctmrnrYbycCoGWbEcj), which is **Ready/Latest** for that exact merge with `www.capitolwonk.com` assigned. No manual deploy was needed. Live [H.R. 7008 Overview](https://www.capitolwonk.com/bills/live-119-hr-7008) shows House passage July 22, Senate receipt July 23, Senate calendar August 6, final passage and law pending. Live [Details](https://www.capitolwonk.com/bills/live-119-hr-7008?tab=details) shows the current in-app overview and August 6 official text source without the older CRS warning. Preview phone-width checks at 390/402 px and exact-head CI preceded the merge. **Next action/owner:** Tyler's installed-iPhone T06 timing/content/layout retest against 6.49 seconds, recorded in the [worksheet](t06-physical-iphone-qa-2026-09-17.md), then Dashboard/Home. Production web smoke is not a substitute for device QA. Keep separate T03/T05/T07/T09/T10–T11 gates.

## September 18 continuation — daily benchmarks and current bill text candidate

- Tyler directed a daily benchmark for every date through the October 30 target, with work pulled ahead when dependencies permit. The [daily benchmark board](daily-benchmarks-2026-09-18-to-10-30.md) now gives each date an evidence target, owner, safe blocked substitute and escalation rule. An 8:30 a.m. local daily heartbeat (`capitolwonk-daily-launch-benchmarks`) was created for this task to work the board and report meaningful progress/blockers; the existing T09 schedule remains limited to its approved scope. The board preserves the October 2–6 owner buffer, October 20–29 review/rework buffer, October 16 candidate checkpoint, October 19 working submission target and exact-action gates. Completion remains evidence-based; the board is a plan, not a claim that a row passed.
- **September 18 benchmark met — Codex, evidence:** Tyler selected a current bill-text overview as the H.R. 7008 Details lead and asked to remove the old CRS text/large notice while it is stale. [Draft PR #46](https://github.com/Tylerandersongates/Capitol-Ledger/pull/46) at exact head `2247fe0` compares the latest Congress.gov text version with the CRS action date across bill Details pages, then shows a source/date-labeled current overview or latest official text in-app. GovInfo EH and PCS substantive act text from enactment clause through House passage matched exactly (14,007 normalized characters, same SHA-256 prefix `07eb4d2ef1da6077`), so the reviewed overview covers both identified printings; a different later text version replaces it. The Overview progress tracker now uses official action dates and labels: H.R. 7008 passed the House July 22, was received in the Senate July 23, and reached the Senate calendar August 6; final passage and law remain pending. [Exact-head CI](https://github.com/Tylerandersongates/Capitol-Ledger/actions/runs/35402726565) succeeded and the [Preview](https://project-qosv1-f9g8upnkj-capitolwonkce.vercel.app/bills/live-119-hr-7008?tab=details) is Ready. The current Summary and progress tracker were checked at 390 and 402 px without horizontal overflow; the older-summary warning is absent. Focused fixtures, syntax and diff checks passed locally. **This candidate is not live; Production remains PR #45.** Congress.gov's latest text and CRS publication are separate streams, so final passage alone does not guarantee a revised CRS summary. **Next action/owner:** Tyler reviews exact head `2247fe0` for the separate Production merge/deploy approval; after an approved release, Tyler retimes the installed-iPhone H.R. 7008 path against 6.49 seconds and checks Details/source layout. If approval or phone time is unavailable September 19, Codex uses that day's Preview/device worksheet substitute. Blocker: Production approval and installed-phone observation, not CI or Preview.

## September 18 continuation — T06 CRS Production release; phone retest pending

- **T06 CRS mobile follow-up:** Tyler approved the stated PR #45 release sequence after reviewing the next steps. [PR #45](https://github.com/Tylerandersongates/Capitol-Ledger/pull/45) merged `f113020c` into `main` as `3098b16` through GitHub's normal merge flow. Exact-head [CI #414](https://github.com/Tylerandersongates/Capitol-Ledger/actions/runs/35395512087) passed; the matching [Preview](https://vercel.com/capitolwonkce/project-qosv1/AhopzD9mUJfGXBqK1HQ4vPVa42kk) was Ready and checked at phone width. GitHub's Vercel commit status remained pending despite that Ready Preview, but GitHub allowed the normal merge. No automatic Production build appeared after the merge, so Codex created a standard Production deployment of the exact merge commit without the paid priority option. [Production](https://vercel.com/capitolwonkce/project-qosv1/DMW7XDPSwUFY3jVfQ3xcFrLLQQ88) is Ready/Latest with `www.capitolwonk.com` assigned. Live H.R. 7008 Details shows the official CRS summary and in-app July 22 photo ID version notice without its raw-text link; live H.R. 9956 Details shows the Summary Unavailable fallback. Every bill Details page now rechecks Congress.gov for a newer official CRS summary; final passage alone does not publish one. **Tyler's installed-iPhone timing/content/layout retest remains open** against the 6.49-second baseline, followed by Dashboard/Home in the ordered matrix. [T06 worksheet](t06-physical-iphone-qa-2026-09-17.md).
- **T09 completed evidence step:** Fresh GitHub and Vercel inspection showed remote `main` at PR #44 merge `617a474` with matching Production Ready and the canonical domain assigned. Public `/support`, `/privacy`, and `/privacy/request` wording was read without submitting a request; the dedicated mailbox is offered and first-party intake is explicitly inactive. The disabled request page's unconditional signed-in-lane card and the source's “Submit and track” metadata need a focused copy correction. The existing local signed build `2` archive embeds Sentry Cocoa `9.22.0` and its privacy manifest; that manifest marks three diagnostic types unlinked, while the provisional App Privacy matrix retains linked diagnostics pending native identifier/event evidence. Details and the manifest hash are in the [T09 packet](app-privacy-release-assets-prep-2026-09-12.md). No questionnaire, provider, Production copy, asset, upload, distribution, or release state changed. Next T09 step: re-read exact current App Store Connect answers and prepare a field-by-field proposed delta, bound to the selected archive's aggregate report and T05/runtime evidence; correct the inactive-intake copy in a separately verified source candidate.
- **T06 remains first:** Tyler's installed-iPhone H.R. 7008 post-#45 timing, Details CRS content and Official sources layout result is still pending. Record it against 6.49 seconds in the [worksheet](t06-physical-iphone-qa-2026-09-17.md), then continue Dashboard/Home first. T03 verifier processing and Notifications V2 stay off. T05, T07, T08 and T10–T11 retain the gates below. October 2–6 and October 20–29 buffers and the October 30 target are unchanged.

## September 18 EOD — T04 closed; H.R. 7008 release verified; T06 phone retest next

This section and the [September 18 EOD](eod-handoff-2026-09-18.md) supersede older rows that call Apple Support or local signing pending. The earlier sections remain dated evidence. Production PR #44 is Ready/Current at merge `617a474`; no TestFlight upload, distribution, App Review submission or release has occurred.

- **Completed since the September 16 EOD:** T04's WWDR G3 trust repair, matching Development profile, signed version 1.0 build 2 archive and iPhone install/launch remain complete. September 17 PRs #35–#38 addressed House election dates, official/Dashboard navigation, cold launch and H.R. 7008 Official sources layout. Tyler reported the House dates appearing on the phone and a cold-launch improvement from 12.74 to 3.49 seconds. PRs #39–#40 deployed the H.R. 7008 source-based policy lens. Tyler recorded a 6.49-second bill-search-to-Overview baseline and found that the July 17 CRS summary omits the photo ID provisions in the July 22 House-passed text. Under separate approvals [#41](https://github.com/Tylerandersongates/Capitol-Ledger/pull/41) added a dated CRS notice and deferred secondary reads; [#42](https://github.com/Tylerandersongates/Capitol-Ledger/pull/42) restored official actions; [#43](https://github.com/Tylerandersongates/Capitol-Ledger/pull/43) refreshed status/latest action; and [#44](https://github.com/Tylerandersongates/Capitol-Ledger/pull/44) kept action-only stored text from replacing the official CRS summary. PR #44's matching Production deployment is Ready/Current. Live browser Overview/Details show **On Floor**, Timeline's latest action is August 6, and Details shows the official CRS summary with the photo ID version notice and official text link. Physical-phone timing after #44 is unmeasured.
- **T01–T02:** closed for documented scopes. Older listing screenshots remain a T09 capture gap. **T03:** high/open; Apple OCSP issue #447 and residual `jsrsasign` acceptance remain separate from the resolved signing problem. Codex monitors/prepares fix evidence; Tyler owns exact remediation and residual-risk decision. September 25 is the fix-path checkpoint; scenario work remains +1–7 working days plus external wait and review. App Store server verification and Notifications V2 remain off. **T04:** local signing/archive/install closed September 16 with no repeat work; export/upload remain T10 gates.
- **T05:** native Sentry event/delivery proof open (Codex/Tyler, historical 1–2 working days after exact protected-device scope). **T06:** installed-iPhone sign-in, relaunch, one saved item, Officials, Begich House dates and H.R. 9956 Basics passed on Tyler's reports; official opens and repeats remain variable, H.R. 7008 post-#44 speed/content/layout retest and the ordered Dashboard/Home, Account, Search, civic, Privacy/Support matrix remain (Tyler on phone, Codex diagnosis; historical 1–2 initial working days plus unestimated fixes/retest). The next single action is to retime H.R. 7008 bill-search-result tap to usable Overview against 6.49 seconds, verify Details/Official sources, then return to Dashboard/Home first in the [worksheet](t06-physical-iphone-qa-2026-09-17.md). **T07:** Apple sandbox purchase, restore, renewal, refund, notifications and Team transitions still lack device/sandbox proof (Tyler exact account/action gate, Codex validation; 1–2 working days plus propagation/defects).
- **T08:** Tyler decides whether the first real Daily Brief/video is launch scope and provides content/access; Codex's conditional integration/QA is 1–2 days plus platform wait. Keep the honest placeholder until decided. **T09:** Codex prepares App Privacy, corrected listing captures, provider/retention evidence and the exact upload packet; effort is not reliably re-estimated. Tyler owns final questionnaire/remote asset/upload decisions. Dedicated mailbox intake is the launch path; first-party privacy queue/operator/monitoring/retention/deletion remain off and conditional post-launch. Do not retry the retired September 15 principal-ACL inventory. **T10:** no TestFlight upload, invitations, public link or App Review submission; Codex prepares (historical 0.5–1 hands-on day) and Tyler approves each exact action; Apple wait unknown. **T11:** October 30 target only; Codex prepares final evidence (historical 0.25–0.5 day), Tyler owns go/no-go/release authorization.
- **T09 source-only follow-up:** The [privacy/assets packet](app-privacy-release-assets-prep-2026-09-12.md), [listing draft](app-store-assets/capitolwonk-listing-draft-2026-09-16.md), and [capture manifest](app-store-assets/capitolwonk-capture-manifest-2026-09-13.md) now distinguish the completed local signed build `2` from later Production web source `617a474` and list the missing privacy report, provider/runtime proof and four-screen capture decisions. This is local preparation only; no questionnaire answer, image, provider path or remote asset changed. Next T09 step: re-read exact App Store Connect answers and prepare a field-by-field proposed delta against the provisional matrix, then bind it to the selected archive/runtime evidence before any publication decision.
- **Forecast and dates:** September 25 T03 decision; October 1 stable-candidate/T08–T09 handoff; October 2–6 protected owner-availability buffer with no required approvals/device/uploads; October 16 candidate checkpoint; October 19 working App Review submission target; October 20–29 review/rework contingency; **October 30 unchanged launch target**. These intermediate dates are internal working targets, not guaranteed Apple timing. The older 7–13-day whole-project estimate is historical after signed-candidate progress and new QA work. No defensible ahead/on-track day variance or total remains because T09, T03 wait, fixes, Apple propagation and review are unestimated. October 30 remains possible but low-confidence and materially at risk. Deferred first-party operator, outbound Brief delivery, Supreme Court sister app and state pilot stay outside October scope. No date change or launch gate was approved today.

## September 17 T06 — House dates passed; cold launch improved, navigation retest open

- Tyler reported that the previous first-elected/next-election repair appears to work for Senators but not U.S. Representatives. The live browser `/members/B001323` shows both fields as `Not listed` for Rep. Nicholas J. Begich. This is a browser-confirmed defect; physical-iPhone confirmation of this particular field is not separately recorded.
- [Source and official-record research](member-election-history-research-2026-09-17.md) found PR #31 only applied an existing 14-ID fallback. Tyler approved merging [PR #35](https://github.com/Tylerandersongates/Capitol-Ledger/pull/35) at head `eaae982`; its Clerk-based 433-member House snapshot and limited next-regular-election rule merged as `3c01b1d`. PR and merged-main CI passed, Vercel marked the exact production deployment Ready/Latest, and the public browser `/members/B001323` rendered **Nov 5, 2024** and **Nov 3, 2026**. Tyler reported both dates appearing on the phone. Additional production browser checks passed Aderholt (regular), Gomez (special), Case (returning) and Higgins (Louisiana runoff). Case's separate tenure counter shows 13 terms while the Clerk lists 7; diagnose that in a distinct source repair. The [remaining T06 device matrix](t06-physical-iphone-qa-2026-09-17.md) remains open.
- **T06 navigation performance remains open:** Tyler reports long first opens of official pages and slow returns to Dashboard on the physical iPhone over Wi-Fi; repeat official opens feel faster. Source inspection found request-time Congress.gov/profile/legislation/vote reads with process-local 10-minute caches on official pages and fresh database/account reads on Dashboard. Under Tyler's separate action-time approval, [PR #36](https://github.com/Tylerandersongates/Capitol-Ledger/pull/36) at `52a026d` merged as `a6ab070`: [main CI #395](https://github.com/Tylerandersongates/Capitol-Ledger/actions/runs/35252045479) passed and the [exact Vercel deployment](https://vercel.com/capitolwonkce/project-qosv1/86MiK5TW6MibDSzQr3pNeZBg7Tpq) is Production Ready/Latest for `www.capitolwonk.com`. The release bounds optional official live reads, caches only public Dashboard records, overlaps independent account/public reads and shows loading shells. Signed-in live browser checks retained official dates, saved items and bill data, but one official still took roughly 6–7 seconds and two Dashboard returns roughly 7–11 seconds. These are browser samples rather than phone percentiles or a controlled comparison. **Next T06 gate:** Tyler retests first/repeat official opens and Dashboard return on the phone, with approximate waits and saved-data checks; if still slow, Codex profiles the account/database path and prepares another focused source repair before the remaining [device matrix](t06-physical-iphone-qa-2026-09-17.md). The October 30 target remains at risk until this and independent launch gates close.
- **T06 cold-launch baseline and production repair:** Tyler measured **12.74 seconds** from a fully closed iPhone app to Dashboard. The native shell loads the root route, then Dashboard, both of which read session state. Fresh Production instances constructed Prisma clients repeatedly and ran sequential schema DDL before reads of migrated auth, subscription and Team tables; live logs also showed repeated account-ledger traffic. Tyler separately approved [PR #37](https://github.com/Tylerandersongates/Capitol-Ledger/pull/37) at tested head `c1ab549`. It reuses a Prisma client per instance, removes schema setup from these migrated read paths and quiets internal favorite reconciliation. [Exact-head CI #396](https://github.com/Tylerandersongates/Capitol-Ledger/actions/runs/35255605291) and Preview passed. Merge `107c8aa` passed [main CI #397](https://github.com/Tylerandersongates/Capitol-Ledger/actions/runs/35256391732) and the [exact Vercel deployment](https://vercel.com/capitolwonkce/project-qosv1/9VPtvDsFfctneQeGQeoYxacvq19h) is Production Ready/Latest for `www.capitolwonk.com`. Signed-in live browser root launch, Begich profile and Dashboard return worked, with 54 active bills and five saved items after hydration, but sampled at roughly 9, 7 and 4 seconds. These are single desktop browser observations, not a controlled phone comparison. **Next T06 gate:** Tyler retimes full iPhone close/reopen on Wi-Fi against the 12.74-second baseline and first/repeat official open plus Dashboard return, confirming saved state; Codex records results and continues the [ordered device matrix](t06-physical-iphone-qa-2026-09-17.md). Performance remains open until the phone retest passes.
- **Post-PR #37 phone result:** Tyler reports 3.49 seconds from fully closed iPhone app to Dashboard, versus 12.74 seconds before the repair: 9.25 seconds, or about 73%, less for this single attempt. The network condition and timing method were not independently reconfirmed. **Next T06 action:** Tyler times first and repeat official opens and the return to Dashboard on the iPhone, checks saved items, then continues the Dashboard/Home row of the [ordered device worksheet](t06-physical-iphone-qa-2026-09-17.md). Codex records any remaining delay and diagnoses a concrete failing path. This reported improvement does not close the full T06 matrix.
- **Official navigation timings:** Tyler reports **4.25 seconds** for a new official first open and **1.71 seconds** to return to Dashboard on the iPhone after PR #37. The return meets the under-2-second target; first open is just over the provisional 4-second investigation mark. These are single timings for an as-yet unidentified official; repeat-open timing remains pending. Keep navigation performance open and use that next result to focus any further diagnosis before changing source.
- **Ami Bera phone sequence:** First open **4.65 seconds**, Dashboard return **1.68**, repeat opens **0.2**, **2.87**, **1.45**, and **2.07 seconds** after two minutes idle. Tyler confirmed the 0.2-second result was a separate reopen. Dashboard return meets the under-2-second target, while first and some repeat opens exceed provisional targets. Source review found member detail followed by up to 2.5 seconds of optional live enrichment, with process-local caches; which stage dominated these phone requests is not yet measured. Continue the ordered Dashboard/Home, Account/Settings, Search, civic and Privacy/Support matrix without displacing it. Codex profiles the official route stages before proposing another repair.
- **H.R. 7008 mobile Official sources repair:** Tyler's screenshot showed metadata overflowing fixed 248 px cards. Tyler approved [PR #38](https://github.com/Tylerandersongates/Capitol-Ledger/pull/38) at `fe857a6`; merge `5b3a7ca` passed [main CI #399](https://github.com/Tylerandersongates/Capitol-Ledger/actions/runs/35261638245) and reached [Vercel Production Ready/Latest](https://vercel.com/capitolwonkce/project-qosv1/CMPyXLH4XTJxpk6UnD6peyibwCzV) on the canonical domain. At 402 px, a signed-in live browser shows the first affected cards growing to 313.5/335.5 px, containing their text and leaving 12 px between cards; visual inspection found no overlap. Tyler's installed-iPhone retest remains open. Continue the ordered T06 matrix without displacement.
- **H.R. 7008 Plain-language view finding:** The live benefits/drawbacks exactly match the generic deterministic fallback and claim a limited official record despite a substantive CRS summary. The server-side AI agent exists in source, but live calls require an entitled plan plus `CAPITOL_LEDGER_AI_BILL_ANALYSIS_PROVIDER=openai` and `OPENAI_API_KEY`; neither variable name is configured in the Vercel project's Project or linked Shared list. The fallback has no stock-trading category. Codex owns a focused source-grounded fallback/content-label repair proposal; Tyler owns any later paid live-agent activation and exact protected configuration/Production gate. Keep the T06 Dashboard/Home-first matrix in order.
- T03, T05, T07, T09 and T10–T11 retain their separate gates. October 30 is still a target, not release authorization. The September 16 final EOD below remains the authoritative prior checkpoint where not updated here.

## September 16 final EOD — live Bill Basics repair; physical-device and release gates remain

This section supersedes earlier September 16 “pending” or “not approved” statements. The [dated EOD](eod-handoff-2026-09-16.md) has the full evidence and preservation state; the detailed task rows below retain historical estimates rather than independently verified new totals.

- **Completed today:** T04 local signing chain repair, valid Development profile, strict/deep-strict signed archive and in-place physical-iPhone build `2` install/launch all passed under separate exact approvals. For T06, Tyler signed out/in, force-quit/reopened to the dashboard without credentials, opened an official after the approved [Officials fix](https://github.com/Tylerandersongates/Capitol-Ledger/pull/33), and found one saved item still present after a full close/reopen. The corrected pooled production database path restored account reads. Tyler approved the distinct [Bill Basics fix](https://github.com/Tylerandersongates/Capitol-Ledger/pull/34); its merge `df0dbcc` is Vercel Production Ready/Latest and the public [H.R. 9956 page](https://www.capitolwonk.com/bills/live-119-hr-9956) shows sponsor, introduction date and committee. Tyler then reported the live bill test verified on the installed iPhone.
- **T01–T02:** closed for their documented web and monitoring scopes; no remaining work in those IDs. Old listing images are a T09 capture gap.
- **T03:** high/open. Codex watches Apple's OCSP issue/fix through the September 25 checkpoint; Tyler owns any exact security-path and residual-risk acceptance decision. App Store server verification and Notifications V2 stay off. Scenario effort remains +1–7 working days depending on official fix/backport/replacement, excluding review and wait.
- **T04:** local signed archive and device-install gates complete. Existing certificates/private keys, the App Store profile and checked-in signing setting were preserved. No export or upload. Do not repeat completed signing work.
- **T05–T07:** T05 native event/delivery proof remains 1–2 working days after exact protected device/event scope; T06 core-screen/device QA remains open despite the passing sign-in, relaunch, Officials and one saved-item checks (roughly 1–2 days for remaining matrix, plus any fixes/retest; Codex and Tyler on the phone); T07 Apple sandbox transitions remain 1–2 days plus propagation and an exact account/action gate. Source or public-page checks do not close physical-device or sandbox acceptance.
- **T08:** Tyler still decides whether a first real Daily Brief/video is in launch scope and supplies the inputs; Codex's conditional integration/QA is 1–2 days plus platform wait. Keep the honest placeholder until decided.
- **T09:** Codex still needs the bounded App Privacy/provider/retention and corrected-wordmark asset evidence; Tyler owns final remote questionnaire/assets and exact upload gates. Effort is not reliably re-estimated. Dedicated mailbox remains launch intake. First-party privacy operator/intake/monitoring/retention/deletion remain off and conditional post-launch; the retired ACL inventory stays retired.
- **T10–T11:** no TestFlight upload, tester distribution, review submission or release. Tyler owns each exact action and final go/no-go; Codex prepares the packet. Known hands-on estimates remain 0.5–1 day for T10 and 0.25–0.5 day for T11, plus unknown Apple processing/review. Deferred outbound Brief delivery, Supreme Court sister app and state pilot stay out of this launch path.
- **Forecast:** The old 7–13 hands-on-day baseline is historical, not a defensible current total after the signed-candidate progress and new T06 repairs. Known remaining ranges above exclude T09, T03 external wait, QA fixes, Apple propagation/review and decision time, so no precise ahead/behind day variance can be claimed. October 1 stability, October 19 planned submission, October 20–29 review/rework and October 30 target are unchanged; keep October 2–6 free of required Tyler approvals/device/uploads. **October 30 remains possible but low-confidence and materially at risk** until T03 acceptance and T05–T09 evidence close in time for the planned submission. Do not move the target or compress gates without Tyler's decision.
- **Tomorrow's first lane:** complete the remaining T06 dashboard, account, core civic, privacy and support screens in order. Then follow T05 native proof, T07 sandbox, T09 release evidence, T08 scope, and T10–T11 delivery gates as dependencies allow. Do not begin those tracks during this EOD.

## September 16 latest — T06 Officials and saved-item retests passed; Bill Basics fix live

- Tyler confirmed an official profile opens on the signed-in physical iPhone after the approved Officials fix reached Production. He saved one item, fully closed and reopened the app, and confirmed the item remained saved. Sign-in, controlled relaunch, official detail, and one saved-item persistence check now pass for installed build `2`.
- Tyler then found H.R. 9956 (Seizure Safe Schools Act of 2026) showing no sponsor, no cosponsors, “Date pending,” and “Committee not listed yet” in Bill Basics while the timeline showed a July 27, 2026 action. The [official GovInfo record](https://www.govinfo.gov/app/details/BILLS-119hr9956ih) names Valerie P. Foushee as sponsor, July 27 as the introduction date, and the House Committee on Education and Workforce. Its displayed page does not establish a cosponsor count. The live app page reproduced the three known missing fields.
- Source inspection found the database bill mapper dropped introduction date and committee and the detail page returned a sparse catalog row without fetching the full official bill details. A focused source repair on `codex/t06-bill-basics-sept16` / `5d7e16c` fills missing Basics fields from stored raw detail or a bounded Congress.gov detail request and derives the named committee from official actions. It leaves production data and configuration untouched. The H.R. 9956 fixture, existing bill detail/action checks, and diff check passed. [PR #34](https://github.com/Tylerandersongates/Capitol-Ledger/pull/34) passed 3/3 GitHub checks and Vercel Preview; Preview H.R. 9956 and H.R. 9955 both showed the official sponsor, July 27 introduction, and Education and Workforce committee. Tyler approved this exact Production merge. GitHub merged it as `df0dbcc`; the matching Vercel Production deployment reached Ready/Latest on the canonical domain. A signed-out [live H.R. 9956 page](https://www.capitolwonk.com/bills/live-119-hr-9956) shows Rep. Valerie P. Foushee, Jul 27, 2026, and Committee on Education and Workforce. Tyler subsequently reported the live bill test verified on the installed iPhone. This general read-path fix is not a claim that every bill has complete source data; cosponsor handling was not changed.
- **Next T06 sequence:** continue the remaining dashboard, account, civic, privacy and support screens in order. Keep T03 verifier off and T05/T07 protected device and sandbox gates separate.

## September 16 latest — T06 relaunch passed; Officials blocked by detail-page error

- Tyler explicitly signed out and back in on the physical iPhone; he reported sign-in working. He then swiped the app away and reopened it to his dashboard without signing in again. The controlled session-persistence check passes. The reversible saved-item persistence check is still pending.
- Opening Search > Officials was slow, and tapping an official led to the app's “Something went wrong” screen. Exact-deployment logs showed a burst of member-detail requests and repeated `AccountPersistenceUnavailableError` with scope `getAccountLedger` from the member page. Source inspection found an unconditional call to the process-memory ledger after a successful database read; production correctly forbids that memory store. Search also left automatic prefetch enabled on every official result link.
- [PR #33](https://github.com/Tylerandersongates/Capitol-Ledger/pull/33) from `codex/t06-officials-response-sept16` / `6cca36d` makes the memory fallback lazy and disables official-result prefetch. Member-profile, search-scroll, account-persistence, TypeScript and diff checks passed locally; GitHub's full CI quality/build job and Vercel Preview also passed on the exact head. Tyler then approved this exact production change. GitHub merged it as `8966be4`, and the matching Vercel Production deployment reached Ready/Latest on the canonical domain. A signed-out live official page loaded its public record. Signed-in iPhone confirmation is still pending; next retest opening and saving one official, relaunch persistence, and the remaining core screens.

## September 16 — corrected production URL restored phone sign-in

- Tyler personally saved the current Neon pooled connection string with Prisma-compatible `Capitol%20Ledger` URL pathname after a non-secret structural check; the password was never displayed or retained by Codex. Tyler launched a new Production deployment of unchanged source `9f058fd`. It became Ready/Latest on the canonical domain. The installed iPhone app, previously verified as signed build `2`, reached that exact deployment through its configured web origin. Two phone sign-in requests returned HTTP 200; session, profile, ledger, gamification and subscription account reads also returned HTTP 200. The earlier account-data HTTP 503 sign-in blocker is resolved for this observed path.
- Tyler saw the sign-in screen again after entering the app, then returned without entering a password and reported the app open. This suggests navigation history rather than a proven logout: the sign-in page does not redirect an existing session automatically, and the native WebView permits back gestures. A controlled force-quit/relaunch and a saved-data persistence check are still needed before marking session persistence passed. No account record, provider setting, source, or native binary was changed during this diagnosis.
- Repeated `/api/account/subscription/app-store` HTTP 503s are the route's intentional `APP_STORE_SERVER_VERIFICATION_DISABLED` result while T03 processing remains off. Source inspection found no sign-out behavior on that response. Do not enable Apple verification or treat this as a database failure; assess any native retry behavior in T05/T07 after the T06 account path. Tyler still needs to change the previously exposed QA password personally when convenient, without sharing it in this task.
- **Next T06 step:** confirm a real app relaunch retains the session, then one reversible saved-account change survives relaunch. Continue dashboard, account, civic, privacy/support QA in order. Keep protected native, sandbox purchase, and deletion gates separate.

## September 16 later night — copied Neon URL does not target the Prisma app database

- Tyler saved a replacement Vercel `DATABASE_URL` and redeployed source `9f058fd` to Production. The new deployment became Ready/Latest on the canonical domain; Vercel shows the project secret updated immediately before that deployment. The iPhone retest still returned the account-data-unavailable message. Exact-deployment logs show `/api/auth/sign-in` HTTP 503 after about five seconds, plus HTTP 503 from password reset and App Store subscription persistence. This remains a database-wide account-persistence block; no password verdict or signed-in QA is possible yet.
- The pre-save format check found the copied Neon Connect URL used pathname `Capitol%2520Ledger`. I incorrectly accepted that form. The September 12 [constrained-restore drill](neon-constrained-restore-drill-runbook-2026-09-12.md) directly tested this same Prisma/Neon naming trap: `%2520` reported a nonexistent database, while changing only the URL pathname to `Capitol%20Ledger` reached the literal application database and passed the target guard. The copied URL path is therefore the leading, concrete explanation for this retest; the masked Vercel value and current database error detail are not exposed, so final confirmation requires a corrected redeploy and real retest.
- **Next T06 gate:** Tyler re-enters the current Neon pooled connection string in the same Vercel secret, correcting only the database pathname from `Capitol%2520Ledger` to `Capitol%20Ledger` before personally saving it. Preserve the current host, user, password, query settings, and Production/Preview scope. Codex can check only non-secret structure before Save. After Tyler's exact approval for a new production redeploy of unchanged `9f058fd`, verify Ready/Latest and the iPhone sign-in response. Stop if the path or scope differs, or if 503 persists; then gather a sanitized current error category before any further configuration change. No production SQL or provider mutation occurred in this diagnosis.

## September 16 night — T06 retest still blocked at account data

- **T06 retest result:** Tyler's build `2` sign-in displayed the new generic account-data-unavailable message. Exact-deployment Vercel logs confirm three sign-in HTTP 503s after merge; a separate account-persistence route failed too. The Production/Preview project secret `DATABASE_URL` is currently visible by name in Vercel, with May 28 add metadata; Tyler reset the Neon production owner password September 15. A stale credential is the leading hypothesis if the secret uses that role, but its masked value was not read and the root cause remains unproven. This contradicts the September 15 read-only record that reported no such project variable. Neon compute is present and active; it scales to zero after five idle minutes. No SQL, secret reveal, credential/configuration edit, or redeploy occurred in this diagnosis.
- **Single next T06 gate:** Tyler personally replaces the Vercel `DATABASE_URL` value with the current Neon connection string for the production application database without sharing it with Codex. After it is saved, obtain exact approval to redeploy current source `9f058fd` so Vercel picks up the changed value, then inspect runtime status and conduct a private sign-in retest. If still 503, diagnose the specific connection failure before another change. The QA password exposed in the earlier screenshot must be changed by Tyler when the account path permits.

## September 16 evening — approved auth repair deployed, T06 retest pending

- Tyler approved the exact [PR #32](https://github.com/Tylerandersongates/Capitol-Ledger/pull/32) production merge after its reviewed head `91e49c6` passed CI and Preview checks. GitHub merged it as `9f058fd`; Vercel's matching Production deployment reached **Ready**, was marked Latest, and listed the canonical domain. The public `/sign-in` page loaded. No credentials, account action, protected query or provider setting were used for this smoke.
- The deployed code now masks auth persistence errors, returns retryable HTTP 503, and clears a rejected schema-initialization cache so a later request can retry. Deployment readiness and a signed-out page load do **not** prove application database connectivity or successful sign-in. The earlier database failure's cause remains unknown.
- **Single next T06 action:** Tyler personally changes the exposed QA password when the account path permits, then privately retries sign-in on the installed build `2` without sharing credentials. Codex checks the resulting status and continues relaunch/persistence and core app QA if sign-in succeeds; if a generic 503 appears, diagnose database connectivity without treating it as a wrong password. T05 monitoring and T07 sandbox follow in the existing order. The October 30 target, September 25 T03 checkpoint and release buffers are unchanged.

## September 16 late afternoon — T06 sign-in blocker and focused repair

- Tyler's physical-device sign-in attempt displayed a database reachability error before the app could establish whether the credentials were valid. A matching production sign-in request returned HTTP 500. The deployed handler exposed the raw database error to the screen, and a rejected auth-schema initialization promise could remain cached in a warm function. Neon's project console later showed the production compute active after an earlier idle state; that does not establish successful application connectivity or a root cause. No production database query, credential, provider setting, or account record was changed during this diagnosis.
- Source-only [draft PR #32](https://github.com/Tylerandersongates/Capitol-Ledger/pull/32) at `91e49c6` masks auth persistence failures behind a generic HTTP 503 with retry guidance, allows auth-schema initialization to retry after a transient failure, and displays the unavailable message as an error. A simulated outage/recovery check, TypeScript, targeted lint, diff checks, exact-head CI and Vercel Preview checks passed on its isolated branch. The fix has **not** been merged or deployed to production; these checks do not establish production database health or successful device sign-in.
- **Next T06 gate:** review the exact PR and CI, then obtain Tyler's action-time approval for a materially behavior-changing production merge/deployment under the [Standing Rules](eod-handoff-template.md). After the deployed fix is verified and database connectivity is healthy, Tyler can change the exposed QA password through the account UI and privately retry sign-in on the phone. Codex then resumes the same T06 relaunch/persistence and core app matrix. T05 monitoring, T07 sandbox, and T08–T11 remain in the existing order; the October 30 target and buffers are unchanged.

## September 16 continuation — ordered October 30 path after signing repair

- The saved project checkout was on `main` at `b2e11b4` (July). Its full status scan stalled; no tracked file there was overwritten. The September 16 source worktree remains intact at `codex/member-service-live-fallback` / `93795c7`, including all 12 local EOD documentation changes. A separate checkout at `/Users/tylergates/Documents/Capitol Ledger/.worktrees/sept16-t09` uses `codex/sept16-t09` from that feature commit. `origin/main` was fetched read-only to merged PR #31 at `90f8b85`; the app, native, dependency and script files are byte-identical between this checkout and that current main. This branch's differences are documentation. No push or PR was made for this continuation.
- **T09 source-only progress:** PR #30 had already corrected mailbox copy and the provisional four-screen plan. The new [listing metadata and capture-state draft](app-store-assets/capitolwonk-listing-draft-2026-09-16.md) specifies provisional public copy, canonical support/privacy URL candidates, and honest signed-out routes/states for four local rehearsal slots. The capture manifest now links it; the App Store Connect setup packet no longer recommends stale Vercel URLs or a demo bill ID and flags its old App Review note as historical. No image, remote metadata, questionnaire, provider state, gate, signing, or device state changed. Final images and answers remain blocked on the exact candidate and evidence/approval gates.
- **T04 local signing repair:** Apple's general rule about genuinely revoked certificates did not explain this Mac's warning. Read-only checks found missing-issuer error `-25318`, two Development identities but zero valid, and only an expired WWDR intermediate locally. Apple Developer Technical Support documents the symptom and Apple identifies WWDR G3 as the Development intermediate. Tyler gave exact approval to import only Apple's verified public G3 intermediate into the login Keychain at default trust. Both existing identities now validate under code-signing policy (**2 of 2 valid**) and offline three-certificate chains. No leaf certificate, private key, profile, Xcode signing setting, or Distribution Managed asset changed. The [signing record](apple-signing-reconciliation-2026-09-10.md) contains the checks and action. A signed build and device QA remain separate gates; a further Support call is not needed for the resolved local chain gap.
- **T04 candidate preflight:** Xcode 27.0 is now selected, superseding the earlier Xcode 26.6 toolchain observation. The shared native Release scheme, automatic-signing configuration, version `1.0` build `2`, and pinned Sentry Cocoa package resolve. A signing-disabled generic iOS Release build passed on this exact source with no compile errors. Free disk space was approximately 3 GiB before and after the build; reuse the existing project package cache, monitor free space, and avoid an unbounded second copy for a signed archive. No signed archive, device installation or upload was made.
- **T04 approved archive attempt:** Tyler approved one local signed Release archive with existing signing and no provisioning updates or upload, then freed drive space. Xcode 27 saw two valid Development identities and about 5.2 GiB free, but the single `xcodebuild archive` exited 65 before creating an archive: no matching **iOS App Development provisioning profile** was installed. Xcode explicitly requested `-allowProvisioningUpdates`, which was outside the approved action, so there was no retry. Free space remained above 5 GiB. The one local profile previously inspected is App Store class; it cannot satisfy this Development profile requirement. One physical iPhone is now available to Xcode, and the signed-in enrolled team has no registered devices in its Developer portal. No certificate, profile, project signing setting, device, upload, or Apple account state was changed. T04 remains open at the device/profile decision gate.
- **T04 signed candidate completed locally:** Tyler approved the exact one-iPhone registration, Development-profile creation/download, and local archive retry. Apple confirmed registration of the connected iPhone; the new profile contains the existing explicit App ID, one existing valid Development certificate, and that device. The original App Store profile was preserved. Xcode did not pick up the portal-created manual profile under automatic signing without broad provisioning updates, so the successful archive used a temporary command-line manual profile selection without changing the checked-in project. The Xcode 27 Release archive for version `1.0` build `2` passed strict and deep strict signature verification; its app ID, team, and embedded unexpired Development profile matched the project and generated profile by boolean-only checks. It is local at `/Users/tylergates/Library/Developer/Xcode/Archives/2026-09-16/CapitolWonk-1.0-2-local.xcarchive`; free space stayed above 5 GiB. No device install, export, upload, or distribution occurred. The [signing record](apple-signing-reconciliation-2026-09-10.md) has the exact evidence. T04's local signed-archive gate is complete; T05–T07 signed/device and sandbox proof remain open.
- **T06 approved physical install and first smoke:** Tyler separately approved replacing the existing CapitolWonk `1.0` build `1` installation with the signed `1.0` build `2` candidate and launching it. The connected physical iPhone had one developer-installed build `1` with an accessible data container. With 5.5 GiB free, normal macOS-access checks found two valid signing identities and passed deep strict archive verification; a sandboxed CLI check had falsely reported the identity untrusted. `devicectl` installed build `2` in place, a readback showed one build `2` installation with an accessible container, and launch succeeded. A one-time device screenshot confirmed the CapitolWonk sign-in screen; the temporary screenshot was deleted. No credentials, account data, protected event, sandbox purchase, export, upload, or distribution were used. Existing app data persistence beyond container accessibility and all signed-in QA remain unverified. Free space was about 3.4 GiB after the install.
- **T03 watch:** Apple's [issue #447](https://github.com/apple/app-store-server-library-node/issues/447) remains open with no linked fix, and Apple's [release list](https://github.com/apple/app-store-server-library-node/releases) still shows `3.1.0` as latest at the September 16 read. Keep App Store server processing off. Recheck the official fix at the September 25 decision checkpoint; do not start a backport or accept the residual verifier risk silently.

### One focus queue

| Order | Work block | Exit condition / next gate |
| --- | --- | --- |
| Done. T04 signed candidate | Keep the verified local archive and generated Development profile for review. | Signature, app ID/team, version/build and embedded profile passed; T06 records the later device install. No upload. |
| 1. T05–T07 signed/device proof | Build `2` is installed and opens to sign-in on the registered iPhone. Continue sign-in/relaunch/persistence and core app QA, then native monitoring and Apple sandbox transitions on this candidate. | Tyler signs in on the phone with the intended QA account. Protected-value/event, account deletion and sandbox actions retain their separate gates; first launch alone does not establish app-level acceptance. |
| Checkpoint. T03 verifier acceptance | Check Apple's official issue/release at the September 25 checkpoint; evaluate an actual fix against the source-free acceptance matrix if one appears. | Keep processing off until the reviewed fix, independent acceptance, and Tyler's security decision. Avoid daily context switching. |
| 2. T09 release evidence | While a protected gate is pending, finish one bounded source/read-only pass for canonical support/privacy pages, provider facts, App Privacy answers and four corrected-wordmark captures. | Review final claims against the signed candidate; remote questionnaire/assets need separate approval. Keep the first-party operator path deferred. |
| 3. T08 scope | Obtain the first real Daily Brief/video inputs and decide whether an episode is in the launch candidate. | If included, verify player/privacy/captions/sources; if deferred, keep the honest placeholder. |
| 4. T10–T11 delivery | Prepare the exact build/audience/review packet, then TestFlight, App Review and launch decisions. | Separate upload, distribution, submission and release approvals; preserve October 2–6 and October 20–29 buffers. |

T04's local signed-archive gate is now closed by the verified archive; T06's install/first signed-out launch step is complete, while T03 and the remaining T05–T11 acceptance work remain open. The October 30 target, September 25 checkpoint, October 2–6 owner buffer, October 20–29 review/rework period, and low-confidence/materially-at-risk forecast remain unchanged. The immediate next step is for Tyler to sign in on the iPhone with the intended QA account, followed by T06 relaunch/persistence and core app checks. Do not resume the retired ACL inventory.

## September 16 EOD reconciliation — current launch boundary

This section preserves the earlier September 16 EOD snapshot of T01–T11. Its open/next-step statements are historical where the continuation above differs, including the now-complete local T04 archive gate. The detailed task rows below retain historical estimates and owners. See the concise [live handoff](HANDOFF.md), the [dated EOD](eod-handoff-2026-09-16.md), and [accepted decisions](DECISIONS.md). The EOD added continuity documentation only; later approved signing actions did not revise the target date.

- **Completed since the prior EOD:** [PR #29](https://github.com/Tylerandersongates/Capitol-Ledger/pull/29) realigned the Oct 30 critical path; [PR #30](https://github.com/Tylerandersongates/Capitol-Ledger/pull/30) merged at `f0bd3aa77a1089ae07ca72a0ffd7bc2d7f196a80` with three checks reported at merge and corrected mailbox-only privacy copy/local asset planning; [PR #31](https://github.com/Tylerandersongates/Capitol-Ledger/pull/31) merged at `90f8b85f6818ca8fdb11ce9e678d6d37439880e7` and fixed live member election-history fallbacks. Its PR head showed 3/3 checks. The prior session verified a Ready production deployment and live `/members/S001150` first-elected/next-election dates. No App Store Connect edit, screenshot upload, privacy gate activation, signed build, or device proof followed.
- **T01–T02:** complete for their recorded web/monitoring scopes; no hands-on effort remains. Two retired-`CE` listing screenshots still need recapture under T09. Do not rerun the closed T02 probe.
- **T03–T04 critical path:** T03 Apple OCSP issue #447, residual `jsrsasign` acceptance and signed/device evidence remain high/open; Codex watches the official fix and Tyler owns the final risk/action decision. T04's local Development identity chain has been repaired under Tyler's exact approval, but an actual signed Xcode candidate and device evidence remain unproven and require separate gates. The prior September 17 same-case escalation trigger for the missing-issuer problem is no longer needed. T03 fix scenarios add +1–7 working days; signed-build and device QA effort still follows the T04 repair.
- **T05–T07:** native monitoring (1–2 days), device QA (2–3 days plus fixes), and Apple sandbox subscription QA (1–2 days plus propagation) await a valid signed/device candidate after T04. Codex owns execution; Tyler owns exact protected/account actions and the device/session. Earlier unsigned/source checks are not substitutes.
- **T08:** first Daily Brief/video launch scope and content remain a Tyler product choice; Codex can prepare integration/QA after inputs (1–2 days plus platform wait). No real player or outbound delivery is implied.
- **T09:** Codex continues source-only App Privacy/listing-metadata/corrected-wordmark reconciliation and read-only provider/retention evidence. Tyler owns questionnaire publication, remote assets and every exact upload/provider gate. The dedicated mailbox is launch intake; first-party queue/operator, monitoring, retention, deletion, and adapter remain off and conditional post-launch. The ambient preflight failed closed and the principal-ACL inventory never ran; do not retry it. T09 remains high/open; no new gate-specific effort estimate is evidenced today.
- **T10–T11:** Codex prepares evidence, Tyler approves exact TestFlight distribution/review and final go/no-go; Apple processing/review is external. T10 remains 0.5–1 hands-on day plus external wait, T11 0.25–0.5 day preparation, both after upstream gates. Neither is authorized.
- **Dates/forecast:** The local signing chain repair removes an external Support dependency but does not yet establish a signed candidate or reduce the established 7–13 hands-on-day baseline, which excludes re-review, remediation, and QA fixes; T03 alternatives add separate scenario effort. The September 17 missing-issuer escalation trigger is retired. Other dates are unchanged: September 25 readiness checkpoint, October 1 stability ceiling, October 2–6 owner buffer, October 19 planned submission, October 20–29 review/rework, October 30 user-set launch target. October 30 remains low-confidence/materially at risk. An exact ahead/behind variance is not measurable from current evidence; do not move the target or compress approval/QA contingency without Tyler's decision.
- **Deferred tracks:** the first-party privacy operator runtime, Daily Brief outbound delivery, Supreme Court sister app, and state-legislation pilot remain outside the current launch path. The September 15 credential containment is closed, but the retired ACL inventory is not a launch dependency. The next dependency-ready local task is the remaining T09 source-only asset/privacy reconciliation after checking what PR #30 already completed.

## September 15 launch-priority and credential-containment correction

This section supersedes the earlier September 15 principal-ACL next-dependency instruction.

- **PR #28 complete:** Documentation/CI-only PR #28 merged into `main` at `e045518`, preserving the failed-preflight evidence and next-step review without activating a runtime path.
- **Catalog inventory retired:** The principal-ACL catalog inventory did not run and must not be retried from this checkpoint. The production operator/ACL/migration/role sequence is conditional post-launch work unless Tyler explicitly reselects first-party privacy queue activation for the October 30 launch.
- **Credential containment complete:** The current Neon owner connection credential was exposed in local terminal output during inventory preparation. The exact-target guard stopped on the wrong/default database; no catalog ACL query or production database mutation occurred. Temporary client tooling was removed and the browser clipboard was cleared. Read-only inspection found no project `DATABASE_URL`, no linked Shared Environment Variables, and no connected Vercel database resource; unknown external clients are not excluded. Tyler directly reset protected production `neondb_owner`; Neon confirmed success with the replacement masked, and Codex did not read, copy, download, or record it. See the [sanitized record](neon-owner-credential-containment-2026-09-15.md).
- **Launch intake boundary:** The configured dedicated privacy mailbox is the launch intake channel. First-party intake, operations, monitoring, retention, deletion, the operator adapter, and database-backed privacy gates remain off. The existing first-party source remains a long-term option, not an October 30 requirement under the current scope.
- **Active Apple-independent queue:** complete source-only App Privacy/listing-metadata/corrected-wordmark asset reconciliation; finish read-only provider/retention evidence; decide the T08 Daily Brief/video launch scope; and watch for the official Apple verifier dependency fix. T03/T04 remain the actual critical path, followed by signed-candidate T05–T07 and then T10–T11. Preserve the October 2–6 owner buffer and October 20–29 review/rework contingency. October 30 remains unchanged, low-confidence, and materially at risk.

## September 15 superseding reconciliation

This is the active T09 and workday boundary. The September 14 reconciliation and older task rows below remain dated evidence, not instructions to repeat completed steps.

- **Completed:** Production Batch A and PRs #8 and #12–#26 are complete for their reviewed scopes. Documentation/CI-only PR #27 merged at `451dc20` with three passing checks, carrying the standing EOD rules and September 14 handoff; it did not activate runtime behavior. OS 27 is installed, and deliberately removed project caches remain regenerable.
- **T09 failed baseline:** The separately approved production ambient-privilege preflight ran once on literal `Capitol%20Ledger` and failed closed: one of two privacy tables existed, and `PUBLIC` database `CONNECT`/`TEMPORARY` were present. Neon protected-branch UI separately confirmed `PrivacyRequestOperation` absent and its source migration name unrecorded. No migration, role, ACL, credential, binding, or gate change followed. See the [evidence](privacy-request-operator-read-only-evidence-2026-09-15.md) and [remediation review](privacy-request-operator-ambient-privilege-remediation-review-2026-09-15.md).
- **T09 next dependency — superseded:** The guarded [principal-ACL inventory](privacy-request-operator-principal-acl-inventory-2026-09-15.sql) remains source-only and unexecuted. Do not retry it. The privacy operator database sequence is conditional post-launch work under the launch-priority correction above.
- **Other carryovers:** T03's Apple OCSP/resource acceptance and residual `jsrsasign` decision remain high/open; T04 signing/device stays frozen pending substantive Apple Support guidance. T05–T07 remain dependent on a valid signed candidate; T08 Daily Brief content/launch scope and T09 privacy/runtime/provider/assets evidence remain open; T10–T11 upload, distribution, App Review, and launch remain unapproved. Privacy intake, deletion, retention, operations, monitoring, App Store server verification, and Notifications V2 stay off. Preserve the October 2–6 owner buffer and October 20–29 review/rework contingency; the user-set October 30 target remains low-confidence/materially at risk, with no date change.

## September 14 superseding reconciliation

This section supersedes pre-merge status statements in the September 13 narrative and task rows below; retain those older statements only as historical evidence until the next full ledger rewrite.

- **Production Batch A is complete.** One approved production migration command applied exactly the five reviewed migrations; Prisma status and the complete postflight passed.
- **Matching default-off source is deployed.** PR #8 merged into `main` at `dca8330e012f723ecd77f2756a865e907c0fa553`, and its Vercel production deployment reached Ready.
- **Verifier containment is deployed but not activated.** PR #12 merged into `main` at `4836e3e48d95d677f8b64713a7f8af62204ea631`; GitHub checks passed and Vercel production deployment `9F3k4yumJTfqDQtEBcAz1bZ6qgnr` reached Ready. `APP_STORE_SERVER_VERIFICATION_ENABLED` and `APP_STORE_SERVER_NOTIFICATIONS_ENABLED` remain off.
- **T03 remains high/open.** The fail-closed containment reduces exposure but does not close Apple issue #447, the OCSP/resource acceptance matrix, independent review, sandbox/device proof, or the residual `jsrsasign` decision. Do not activate App Store server processing.
- **T04 remains blocked.** No substantive Apple Support guidance has been recorded; preserve the certificate, profile, Keychain, signing, and device freeze.
- **T09 is the active Apple-independent track.** Production Batch A, the default-off privacy source, verifier containment, aggregate monitor, owner policy, custom-domain cutover, default-off operator-lifecycle source, pure operator-boundary/pause packet, artifact/restore-floor verifier, parser/dispatcher core, fail-closed stdin shell, explicit-dependency service adapter, least-privilege database-access contract, source-only function boundary, atomic function-migration/ACL review, and inert role-bootstrap review are complete for their exact approved scopes. PR #25 merged into `main` at `8534c7357bd6bc423d8afced5b0fa3015c2be247`; all three pull-request checks passed, exact-source Vercel Production deployment `63yd38aWH3q382bjoundusTdU8Ep` reached Ready and Current with `www.capitolwonk.com`, and the apex redirect, canonical `/privacy` 200, and disabled `503`/`no-store` smoke passed. The next local checkpoint is the source-only ambient-privilege preflight review packet. The adapter remains disconnected from the stdin shell and application routes. No production role exists, the operations and function migrations are not applied, and every privacy, deletion, retention, operations, monitor, and App Store processing gate remains off.
- **September 14 privacy-operations decisions:** the [owner policy](privacy-operations-policy-2026-09-14.md) selects a server-only private-register boundary, twice-business-day review windows, the single-owner pause model, proportionate existing-channel identity verification, and a minimized 24-month closed-case record with shorter payload lifetimes. These are approved for implementation and isolated synthetic validation, not production operation.
- **Custom domain cutover is complete.** Porkbun now routes `capitolwonk.com` and `www.capitolwonk.com` to the existing Vercel project, with the apex returning an HTTPS 308 to canonical `www`. Key public routes passed and privacy intake continued to fail closed. Mail, sender, DKIM, DMARC, ACME, and wildcard records were preserved; see the [September 14 cutover record](custom-domain-cutover-2026-09-14.md).
- **Privacy operator lifecycle source is deployed default-off:** the minimized operations schema, closed contract, server-only service, 15-minute high-risk reauthentication check, 30-day optional-detail minimization, 24-month closed-record expiry, and two-window pause decision are on `main`. The real migration SQL passed the ephemeral PGlite PostgreSQL exercise with runtime-randomized synthetic identifiers and aggregate-only output; the database closed in all outcomes. The production migration was not applied, no public operator route exists, and no protected configuration, mailbox/provider access, scheduler, production read, retention run, or activation occurred.
- **Current checkpoint:** PR #25 deployed the source-only inert role-bootstrap review at `8534c7357bd6bc423d8afced5b0fa3015c2be247`; its three pull-request checks, exact-source Ready/Current Vercel Production deployment `63yd38aWH3q382bjoundusTdU8Ep`, and gate-off smoke passed. The adapter cannot discover a database URL or credential, and the stdin shell and all application routes remain disconnected from it. The next local candidate prepares a guarded, aggregate-only ambient-privilege preflight inside an explicit read-only transaction. It requires an exact expected-database setting, verifies the two privacy-table shapes, checks `PUBLIC` database/schema/table ACLs and reviewed-role name availability, returns no database name, identity, ACL identity, application row, customer data, or credential, and changes nothing. The packet remains outside Prisma migrations and validates only in ephemeral PGlite. Finish complete local checks and review, then present this candidate PR separately. Production target inspection, shared `PUBLIC` ACL remediation, production role creation, credential lifecycle, network origin, protected binding, production migration or grant execution, adapter conversion, real provider/storage deletion evidence, scheduler, production monitor read, production retention run, Vercel change, restore, traffic switch, provider exercise, or activation remains separate approval-gated work. Keep privacy intake, deletion, retention, operations, monitor, and App Store processing off.

Last reconciled: September 13, 2026 after the completed Neon cleanup and local release-preparation pass. Source candidate `92b61b970423ce33dc32f6a2f0a9b72a6d3e2e86` remains isolated in open, unmerged [PR #8](https://github.com/Tylerandersongates/Capitol-Ledger/pull/8) on `codex/sept12-privacy-neon-candidate`; the last independent remote verification covered predecessor PR checkpoint `0c09abc0523e2f4de16f55f4d6d2465d27f4abfa`, with passing exact-head [CI #321](https://github.com/Tylerandersongates/Capitol-Ledger/actions/runs/34780116093) and matching Ready [Vercel Preview `7d1i8rinVS8iUhUTPUyba4DMwUfy`](https://vercel.com/capitolwonkce/project-qosv1/7d1i8rinVS8iUhUTPUyba4DMwUfy). The reviewed documentation-only descendant containing the hardened packet must be verified at its own live PR SHA before use. Production source remains unchanged at `7ec68bcf142d6defe865c12959b0f9a84fce72d5`. The temporary Neon child and compute expired automatically. Final target evidence verified production application reads on literal `Capitol%20Ledger`; with Tyler's separate exact confirmation, only the empty 7344 kB spaced-name database was permanently deleted. Neon now retains `neondb` and application database `Capitol%20Ledger`; post-delete production read smoke passed. No migration, restore, application-row write, candidate merge/deployment, or activation occurred. Privacy intake remains disabled and the verified fallback remains configured. The [five-migration promotion packet](production-five-migration-promotion-packet-2026-09-13.md), [privacy operations/single-owner contingency](privacy-operations-single-owner-contingency-2026-09-13.md), and [local capture manifest](app-store-assets/capitolwonk-capture-manifest-2026-09-13.md) are prepared, but their stated execution and operational gaps remain open. Production Batch A is also incompatible with unchanged `7ec68bc` until the legacy Stripe `team-owner-upgrade` write is reproduced safely and a compatible source/traffic sequence is separately approved. Apple Support still has not replied; Tyler plans to call September 14, and T04 remains frozen pending supported guidance. Use the [September 13 handoff](eod-handoff-2026-09-13.md). This is the current planning source; the older phase detail in `Capitol Ledger App/Next Steps.md` is reference/history, not a fresh remaining-work estimate.

Historical candidate boundary: `a6cb1da` remains the exact September 10 reviewed candidate with passing [GitHub CI run 34557134361](https://github.com/Tylerandersongates/Capitol-Ledger/actions/runs/34557134361), matching Ready Preview `6mKbaWGJ2jdRDbNRaB1K8ZMYKh8L`, and targeted authenticated smoke. Candidate `f4f04de` retains its September 10 dependency-audit closure, and `3dbba3a` retains older historical remote evidence. Current source candidate `92b61b9` remains isolated beneath verified predecessor PR checkpoint `0c09abc`; exact-head CI #321, matching Ready Preview `7d1i8rinVS8iUhUTPUyba4DMwUfy`, and privacy-fallback gate-off smoke pass for that predecessor. The historical `c96662a` / CI #318 checkpoint remains preserved but is superseded by `0c09abc` and the documentation-only packet descendant. The candidate preserves the pnpm 9 lock that forces Next `15.5.25` to PostCSS `8.5.25`; its refreshed production/full audits report **no known vulnerabilities**. Production transitive `jsrsasign@11.1.5` is deprecated/end-of-life and is used by Apple's official server library for X.509/OCSP. Apple's open issue #447 affects the exact enabled online-check path and can defeat OCSP freshness rejection. Do not accept this as maintenance risk alone: wait for an official fix through September 25 or separately approve an independently reviewed backport/replacement and fully revalidate a new candidate by October 1. Even a corrected path that retains `jsrsasign` requires separate time-bounded residual-EOL acceptance. See the [September 12 decision packet](jsrsasign-risk-decision-2026-09-12.md) and [September 11 dependency note](dependency-security-audit-2026-09-11.md).

Production/privacy boundary: the September 11 read-only migration-status and aggregate safety checks found 16 repository migrations and exactly four pending; current candidate `92b61b9` adds `20260912120000_privacy_request_intake`. The exact five-item set and hashes are reconciled in the [September 13 promotion packet](production-five-migration-promotion-packet-2026-09-13.md). **Batch A is no-go against unchanged production `7ec68bc`:** its legacy Stripe Team `checkout.session.completed` path can still write sentinel `workspaceId = 'team-owner-upgrade'`, while migration 3 replaces that convention with a foreign key to `TeamWorkspace`; delayed or retried events can therefore fail after the migration window. Before any production proposal, reproduce this boundary on the replacement isolated child with all Stripe effects mocked, then separately approve either a compatible-source-first sequence or a complete source/traffic-quiescence plus delayed-event reconciliation sequence. Batch A also remains blocked until restore-drill phases 2–5 and isolated verifier/failure validation pass, a direct runner/endpoint binding and lock/window plan are approved, fresh direct-production evidence passes, and a tested recovery procedure is complete. Neon is paid and displayed as `CapitolWonk`, with a setting-verified seven-day history window and protected default `production` branch. The expired Phase 1 child contained literal `Capitol%20Ledger`, `public` with 29 tables, 12 finished migrations, the known resolved retry, exactly five pending candidate migrations, `PrivacyRequest` absent, zero `AccountDeletionRequest` rows, and no completed-deletion watermark. Privacy intake remains behind exact `PRIVACY_REQUEST_INTAKE_ENABLED=false`; offline frozen install, refreshed audits, full TypeScript/lint, complete local-preparation release-source checks, Prisma validation/generation, production build, exact-head CI #321, matching Ready Preview `7d1i8rinVS8iUhUTPUyba4DMwUfy`, and targeted privacy-fallback gate-off smoke pass. Tyler is the sole privacy-request owner; no backup is currently available. The controlled-domain fallback mailbox and sender identities are configured, but forwarding/outbound tests were not sent. The single-owner contingency is documented but not approved or operated; staff resolution/export operations, monitoring, privacy-request retention, real-PostgreSQL exercise, authenticated/runtime-provider evidence, candidate source deployment, and activation remain open. No merge, production migration, restore, App Store Connect, signing, device, upload, distribution, or release state changed; the only production deployment was the approved same-source configuration refresh.

## Schedule baseline and capacity

- **Completed milestone — September 3:** CapitolWonk display-name cleanup and free Daily Brief/Pro layout are live on Vercel at release commit `e82d7ea`; the daily-editions migration is applied. CI and production smoke passed. Logo artwork was not changed.
- **September 4–10 gap:** no tracked product-work evidence was found after `e82d7ea`. The September 10 commit `f37f05e` is only the byte-identical documentation checkpoint from September 3; it does not evidence work during the gap. Do not infer that no off-repository work occurred, but keep every unresolved task unresolved without evidence. Five weekdays/seven calendar days elapsed; do not assume all were available working time.
- **September 10 logo clarification and release:** Tyler confirmed that the existing Capitol dome artwork should remain. T01 was narrowed to removing the visible `CE` suffix from shared wordmark cards. Commit `6ce9d5b` implemented the correction and regression assertion; after Tyler explicitly approved the exact release, `main` advanced to `7ec68bc`. GitHub CI run `34503725976` passed, Vercel production deployment `3DA1NsWFjwPYpFgx1GhhkjGUYMNy` reached Ready/Current, and live dashboard/sign-in/onboarding/map/Brief smoke checks passed. Previously uploaded/listing screenshots remain unchanged and require separate approval before remote replacement.
- **September 10 T02 evidence reconciliation, re-read September 12:** the current ledger had stopped at the July 29 failed `user.geo` rule, but pushed evidence branch `codex/sentry-geo-deep-scrub-verification` contains the superseding July 30 remediation at `46efc95` and July 31 handoff at `d7980aa`. Tyler had approved remediation rather than risk acceptance. Both Sentry projects were changed to the recursive `$user.geo.**` selector, and exactly one authorized Preview Edge event stored neither raw IP nor displayed derived geography. The synthetic issue, temporary route and one-time Preview state were removed, with the cleanup route returning 404. T02 is complete for new events; older events were not retroactively rewritten. The authenticated September 12 pass re-read `$user.geo.**`, Data Scrubber, Default Scrubbers, and Prevent IP as enabled on both projects. No new probe or protected-setting change was made.
- **September 10 T03 audit, remediation and closure:** the baseline Node `22.22.3`/pnpm `9.15.9` production audit reported **2 critical, 11 high and 2 moderate** findings. Tyler approved the narrow patch. Candidate `f4f04de` aligns `next`, `eslint-config-next` and `@next/swc-wasm-nodejs` on `15.5.25` and uses a targeted lock refresh that moves every compatible vulnerable transitive. Normal resolution also advances compatible packages inside the affected Next/ESLint/Sharp/Sentry build subtrees; the direct manifest changes only the three aligned Next pins, with no override or unrelated direct dependency. Frozen install, native/asserted-WASM builds, 38/38 application/readiness validations, 19/19 optimized HTTP checks, 2/2 image-optimizer requests and an unsigned Xcode Simulator Release build pass. Exact-head [GitHub CI](https://github.com/Tylerandersongates/Capitol-Ledger/actions/runs/34518247389) and matching Vercel deployment `mep97jz9t5Hncx1t2mRBeTytDqut` passed. Production/full audits are **0 critical, 2 high and 2 moderate**, all in Next's exact nested `postcss@8.4.31`. Tyler explicitly accepted that changed four-advisory residual for `f4f04de`; T03 is closed for this candidate. Re-audit if its dependency graph changes. See [the September 10 dependency audit](dependency-security-audit-2026-09-10.md).
- **September 10 T04/T05 evidence reconciliation; Apple reply rechecked September 12:** the July 31 handoff supersedes the older signing and native-monitoring summaries. A current read-only pass confirms zero usable code-signing identities. One non-expired Xcode-managed App Store profile exists and matches the project, but no usable certificate/private-key identity is available. Stable Xcode 26.6 remains selected; Xcode 27 beta remains installed but unselected. The paired iPhone record still has Developer Mode enabled but is disconnected, with zero physical mobile devices currently available. Tyler approved one corrected sanitized follow-up on the existing Apple case and Apple acknowledged receipt September 10. The September 12 read-only mailbox pass found no newer substantive Apple reply. T04 remains blocked; preserve the certificate/CSR/Keychain/profile/signing freeze. Take no signing/provisioning action now. If no substantive reply exists at the September 17 slip trigger, prepare one sanitized same-case escalation for Tyler's approval; do not send it or create a duplicate automatically. Protected native Sentry setting resolution and an unsigned Release build passed in July through a temporary mode-0600 settings file, which was removed with temporary artifacts. No native event was sent; the approved one-event scope remains unconsumed pending a valid signed/device candidate and exact-action review. See [the T04 reconciliation](apple-signing-reconciliation-2026-09-10.md).
- **September 10 workspace-integrity resolution:** after the worktree and remote branch matched at `2443c08`, an unexplained uncommitted `pnpm-lock.yaml` diff appeared during the read-only T04 pass. It downgraded several T03-remediated transitives without changing `package.json`. The diff was quarantined; Tyler then explicitly approved restoring only the lockfile to the committed T03 version and pushing the T04 documentation checkpoint. The lockfile now matches the accepted graph, and no dependency change was staged, committed or pushed.
- **September 10 App Privacy preparation, corrected September 12:** while T04 waits on Apple, a source and resolved-SDK audit identified five missing questionnaire types: Emails or Text Messages, Device ID, Crash Data, Performance Data, and Other Diagnostic Data. Sentry Cocoa `9.22.0` creates a persistent installation ID and device-and-app hash, so the provisional matrix marks Device ID and all native diagnostic types linked unless an exact candidate strips and verifies those identifiers absent. The September 12 review preserved the fourteen-type matrix, documented public-copy and privacy-rights/Sentry intake blockers, and audited the exact stale listing/review assets. The [September 12 privacy/assets packet](app-privacy-release-assets-prep-2026-09-12.md) records the corrected decision inputs and safe recapture plan. No source, production, App Store Connect, or remote asset changed.
- **September 10 account-deletion implementation:** replaced the request-only path in the reviewed branch candidate with an explicit in-app action that attempts immediate deletion. One serializable database transaction snapshots deduplicated cleanup jobs for affected Team members and any legacy Stripe subscription, removes the account plus every linked relational/raw-store row in the implemented inventory, verifies zero matches, and deidentifies the completion audit. The deletion operation performs no Team-member/provider mutation before its account commit; a database failure rolls back jobs and deletion together. Production account persistence is exact-ID/read-only, with no missing-user recreation, email remap, or process-memory fallback. After commit, an idempotent worker retries Team-member/legacy-Stripe cleanup; terminal/missing outcomes complete safely, transient failures remain pending, and successful jobs are erased. Signed Stripe webhooks enforce a five-minute timestamp tolerance, reconcile current provider state, reject stale subscription IDs, and, before acknowledgement, stop an active deleted-user legacy renewal and detach metadata where Stripe permits; terminal/missing subscriptions count as complete. Team-seat pause writes serialize on account/workspace locks and attempt Stripe resume compensation if their transaction fails. Rate-limit subjects are hashed/expired and cleared on deletion. Browser completion requires an explicit success receipt; ambiguous results are unconfirmed, and a multi-tab fence blocks browser/native writers from recreating state. The pause-integrity migration converts only known sentinels and aborts on unexpected orphans. The source/migrations remain outside production; live schema/task/real-Postgres concurrency/Stripe/device/backup/provider gates remain under the [runbook](account-deletion-runbook-2026-09-10.md).
- **September 10 EOD/restart boundary:** before final review, branch `codex/logo-refresh-sept10` and its remote both resolved to `2e4e04b` and the privacy/deletion plus channel-only YouTube work was an uncommitted local delta. A read-only corruption scan, complete tracked/untracked audit, secret scan, and consolidated local matrix passed. Tyler then authorized the final branch checkpoint. The reviewed payload was committed as `871ba4e`; exact-head CI exposed one unused parameter binding. Direct correction `a6cb1da` removed only that source binding without changing the accepted input shape and reconciled the handoff wording. Dependency pins and `pnpm-lock.yaml` remain on the accepted T03 graph. The public channel `https://www.youtube.com/@CapitolWonk` is verified and configured with no episodes or player; production is unchanged.
- **September 10 final remote verification:** branch candidate `a6cb1da` synchronized with its upstream. GitHub CI run `34557134361` passed in 1m 50s; its only annotation is the non-blocking GitHub Actions Node 20 deprecation warning. Matching Vercel Preview deployment `6mKbaWGJ2jdRDbNRaB1K8ZMYKh8L` reached Ready. Authenticated preview smoke verified the honest no-video `/brief` state and real Subscribe target with no player, the updated `/privacy` page, and direct `/account-deleted` navigation remaining explicitly unconfirmed. This clears the branch evidence gate only; it is not production deployment, migration, scheduler/provider configuration, account-deletion QA, App Store publication, device verification, upload, distribution, or release.
- **September 11 source-only schedule pull-forward:** committed candidate `3dbba3a` adds the official Apple server dependency, authoritative signed-state/Notifications V2 persistence and reconciliation, fail-closed Apple/Team handling, a fourth migration, default-off deletion and privacy-retention controls, telemetry/log minimization, blank-account demo isolation, expanded fixtures, and an execution-ready [sandbox QA matrix](app-store-sandbox-qa-matrix-2026-09-11.md). Frozen graph, local source, exact migration-preflight, exact-head CI, matching Ready Preview, and targeted smoke evidence pass. Apple and Team behavior still require real sandbox and signed-device proof; deletion/retention gates remain off and unapproved.
- **Launch target — October 30, 2026:** explicitly set by Tyler at September 3 closeout. Work backward from this date; do not silently move it. A forecast change must be surfaced with the blocker, impact and recovery options. This target does not authorize an upload, review submission, distribution or public release.
- **September 10–30:** remaining pre-buffer working window for the gate-driven TestFlight preparation below. Prioritize user-dependent decisions and device work while Tyler is available.
- **October 1:** planning checkpoint for a stable build, current evidence, unresolved issues and the next safe action before the availability buffer. Avoid scheduling a new release/tester expansion that depends on Tyler's immediate follow-up during the buffer unless a separate coverage plan is approved.
- **October 2–6:** reserve a conservative five-calendar-day owner-unavailability buffer based on the stated four-to-five-day absence. Do not schedule required approvals, live device sessions, uploads or review submissions in this window. Confirm whether working days were intended when re-estimating; do not assume an earlier return.
- **October 7 or later:** tentative planning restart, subject to Tyler confirming availability. Extend the buffer if needed; this is not a recovery deadline.
- **Schedule assessment on September 12:** still behind the first internal checkpoint. Visible read-only provider settings, dependency decision inputs, privacy corrections, and asset audit are now prepared, but masked/permission-limited/runtime fields remain open and the work does not remove the critical path. T04 remains externally blocked with no new Apple reply, and issue #447 adds a security fix/revalidation gate to T03/T07. October 30 remains the user-set target, but the forecast is low-confidence and materially at risk until an authorized signing path, a fixed verifier candidate, Apple/Team sandbox results, physical-device QA, provider/runtime evidence, and release approvals are evidenced. If signing remains unresolved on September 17, flag the September 25 readiness checkpoint and October 19 submission target as likely to slip. Do not call the project on track or ahead.
- **Updated remaining effort through September 12:** the prior product/release baseline remains **7–13 hands-on working days** across T04–T11, excluding additional QA fixes, re-review, and external provider/Apple wait time. It does not include the newly required verifier remediation. Current scenarios are approximately **8–15 total** for an official package that satisfies every accepted control (+1–2), **9–17 total** for an official #447 fix plus local controls (+2–4), **10–18 total** for a narrow independently reviewed backport plus controls (+3–5), or **11–20 total** for verifier replacement (+4–7). October 1 remains the stability ceiling and October 30 remains low-confidence because signing, security remediation, sandbox, device, runtime, activation, upload, and review gates are sequential or external.

### Backward plan to October 30

These intermediate dates are proposed internal checkpoints, not claims of completion or automatic approvals. Apple processing/review timing is external and not guaranteed.

| Working target | Milestone / exit evidence | Dependency and contingency |
| --- | --- | --- |
| September 10 | Existing logo retained; shared wordmark corrected and released on `main` at `7ec68bc`. CI, Ready/Current Vercel production and five-route live smoke passed. The App Privacy/deletion source was then branch-verified at exact historical candidate `a6cb1da`. | Preserve this as September 10 evidence only. The September 11 working delta invalidates `a6cb1da` as the current release candidate. |
| September 12–14 | **Implementation, candidate freeze, email refresh, automatic Neon child cleanup, empty-database hygiene, and local packet preparation complete:** Neon paid/name/seven-day/protected settings and constrained-floor Phase 1 are verified. The expired Phase 1 child had the expected five pending migrations, `PrivacyRequest` absent, zero `AccountDeletionRequest` rows, and no completed-deletion watermark. The temporary child and compute expired automatically on September 13. The final read-only target check proved production reads use `Capitol%20Ledger`; with separate exact approval, empty spaced-name `Capitol Ledger` was permanently deleted. Retained databases are `neondb` and `Capitol%20Ledger`, and post-delete production smoke passed. Option 1 privacy intake and the database-target guard are frozen at `92b61b9`; local checks, open PR #8 head `0c09abc`, exact-head CI #321, matching Ready Preview `7d1i8rinVS8iUhUTPUyba4DMwUfy`, and privacy-fallback gate-off smoke pass. The five-migration packet, single-owner contingency, and local capture manifest are prepared. Resend verifies `capitolwonk.com`; four public aliases and the Production/Preview sender/contact configuration are set, and the same production source was redeployed Ready with live `/privacy` smoke passing. Tyler is the sole privacy owner; no backup is available. | Apple Support has not replied and Tyler plans to call September 14. Review the prepared packets. Before Phase 2, separately approve creation of a replacement temporary child/compute, protected credential handling, fresh baseline/target checks, expiry/cleanup ownership, and the mocked `7ec68bc` legacy-sentinel reproduction. Production Batch A remains no-go until one compatible-source or complete source/traffic-quiescence sequence is separately reviewed and approved. Keep PR #8 unmerged while migration, operational, and approval gates remain open. Forwarding/outbound delivery exercises, provider-resource creation, Phase 2 and every later drill, source/traffic changes, candidate deployment, activation, App Store, and destructive action remain separate approvals. |
| September 15–21 | Only after Apple Support clarification plus Tyler's exact approval, establish the T04 signing path and exact signed native candidate. Begin T05 native monitoring proof as soon as that candidate exists. | Support guidance, Tyler's decision, and Apple provisioning remain dependencies. If no supported signing path is approved by September 17, flag the September 25 readiness checkpoint and October 19 submission target as likely to slip. Re-audit T03 if the dependency graph changes. |
| September 22–29 | Complete T05 protected/native checks, T06 physical-device QA, and T07 [App Store sandbox QA](app-store-sandbox-qa-matrix-2026-09-11.md). Resolve findings and freeze the upload candidate. Decide the verifier fallback no later than September 25 if no official fix exists. | September 25 remains the readiness go/no-go checkpoint. Reaching it requires a supported signing path, fixed verifier plan, and meaningful Apple/Team sandbox progress. A backport decision on September 25 leaves only four business days to the October 1 ceiling and competes with T05–T07; source fixtures do not substitute for runtime proof. |
| September 29–October 1 | Decide T08 Daily Brief/video launch scope; complete T09 launch-required archive/provider/App Privacy/metadata/corrected-asset reconciliation and the exact TestFlight upload/distribution decision; complete the availability handoff. The first-party privacy operator database sequence is not launch-required under the current mailbox-fallback scope. | Any future production privacy migration, ACL, role, credential, activation, cleanup-task configuration, destructive QA, or provider mutation remains conditional post-launch work unless Tyler explicitly restores it to launch scope. October 1 remains the pre-buffer stability ceiling. |
| October 2–6 | Protected owner-unavailability buffer. | No required approvals or device sessions. Extend if needed and show the forecast impact. |
| October 7–16 | Resume when confirmed available; finish tester fixes/regression, corrected-wordmark screenshots, final release metadata and video readiness/scope. October 16 release-candidate checkpoint. | T06–T09; real playback still requires a real upload. Confirm whether the first video is included in launch scope if not yet ready. |
| October 19 | Working target to submit the exact release candidate for App Review after all gates and Tyler's explicit approval. | T10; earlier submission can be proposed if genuinely ready. This is not a promise of Apple approval timing. |
| October 20–29 | Review/rework and release-readiness contingency. | Resolve review feedback, retest fixes, verify support and launch coverage; preserve this buffer rather than filling it with new features. |
| **October 30** | **User-set launch target and final go/no-go.** | T11; requires Apple approval where applicable, passing release evidence and explicit release authorization. If a gate is not satisfied, present the impact/options rather than releasing unready. |

Earlier phase estimates were 2–4 days for civic-data expansion, 3–7 for external services and 2–5 for packaging after prerequisites. They describe July scope and include work since completed; do not add them together as time remaining. The September 10 task ranges above replace that arithmetic baseline; re-estimate them when Apple guidance changes T04's duration or another dependency materially moves. Apple processing/review and provider wait time are separate from hands-on effort.

## Ordered carry-forward tasks

| ID | Task / current status | Next checkpoint and dependency |
| --- | --- | --- |
| T01 | **Completed and deployed September 10.** Tyler retained the existing logo artwork. Commit `6ce9d5b` removed the hardcoded `CE` from shared wordmark cards and added a regression guard; production source `7ec68bc`, CI, Vercel and live smoke all pass. | Owners: Codex implementation/evidence and Tyler exact release approval are complete. Remaining T01 hands-on effort: none. Risk: low/closed for current web cards. The two current listing screenshots still show `CE`; recapture them before reuse under T09 and do not alter remote App Store assets automatically. |
| T02 | **Completed; live controls re-read September 12.** The July 29 `user.geo` rule failed, but Tyler subsequently approved remediation. Evidence commits `46efc95` and `d7980aa` show both projects using `$user.geo.**`; one authorized Preview Edge event contained no raw IP or displayed geography, and all temporary probe state was removed. September 12 authenticated evidence confirms both projects still have `$user.geo.**`, Data Scrubber, Default Scrubbers, and Prevent IP enabled. | Owners: Tyler's remediation decision and Codex verification are complete. Remaining T02 effort: none. Risk: low/closed for new events based on the historical one-event verification plus current setting re-read. Advanced rules did not rewrite pre-change events. Do not run a new probe or change a rule without fresh approval. |
| T03 | **Exact frozen graph is advisory-clean; known OCSP defect blocks Batch B and activation.** The old accepted result was 0 critical, 2 high and 2 moderate in Next's nested `postcss@8.4.31`; the pnpm 9 lock now forces that path to `postcss@8.5.25`, and frozen production/full audits report no known vulnerabilities. Production transitive `jsrsasign@11.1.5` is end-of-life and Apple's open issue #447 affects the enabled OCSP freshness path. Tyler approved the documentation-only next step, and the source-free validation/fallback specification is prepared. | Owners: Codex for upstream monitoring/fix evidence; Tyler for any implementation and final risk decision. **Internal release risk: high/open; upstream vulnerability severity is unassigned.** Do not accept `3.1.0` as-is. Wait for an official Apple fix through September 25; if absent, choose and separately authorize an independently reviewed backport/replacement path. Scenario effort is +1–2 for a fully sufficient official package, +2–4 for an official fix plus local controls, +3–5 for a backport plus controls, or +4–7 for replacement. See the [source-free specification](jsrsasign-ocsp-fallback-spec-2026-09-12.md), [September 12 risk packet](jsrsasign-risk-decision-2026-09-12.md), [September 11 dependency note](dependency-security-audit-2026-09-11.md), and [historical September 10 audit](dependency-security-audit-2026-09-10.md). |
| T04 | **Local signed-archive proof complete September 16.** Apple's public WWDR G3 intermediate repaired both existing Development identities. Tyler approved registration of the available iPhone and one matching Development profile; the App Store profile and certificates were preserved. Xcode 27.0 produced a signed Release archive for version `1.0` build `2` on the same source as fetched `main`. Strict/deep strict signature, team, app ID, version/build and embedded-profile checks passed. The archive remains local; no device install or upload occurred. | Owners: Codex completed the local archive and evidence under Tyler's exact approvals. The T04 local gate is closed. Device and sandbox acceptance move to T05–T07; no cloud distribution or App Store outcome follows from this Development-signed archive. See [the signing reconciliation](apple-signing-reconciliation-2026-09-10.md). |
| T05 | Protected service and native monitoring verification — partial. Production browser/auth/feedback evidence exists. A temporary mode-0600 build-settings file previously resolved the protected native Sentry value, and an unsigned Release build passed with a valid resolved value. The signed `1.0` build `2` is installed on the registered iPhone, but no native event was sent, so runtime delivery and intended-environment end-to-end checks remain open. | Owners: Codex for checks; Tyler for the exact device/event action review. The previously approved one-event scope remains unconsumed; verify its exact terms before using it, and seek approval only for a changed scope. Estimate: 1–2 working days after the scoped device action. Risk: high. Resupply the value without exposing it; presence is not proof. No outbound Brief sends or scheduler activation. |
| T06 | Physical-device QA and targeted polish — **signed build `2` installed; focused sign-in, relaunch, saved-item, Officials, Bill Basics, and House election-date checks passed on Tyler's phone reports; remaining screen matrix open.** Production browser checks back the relevant public pages, but do not close other device flows. | Owners: Tyler for the phone walkthrough and any later account/deletion approval; Codex for recording results, diagnosis and repairs. Estimate: 1–2 working days for the remaining matrix, with fixes/retest extra. Risk: high. Next screen is Dashboard/Home, then Profile/Account, Search Bills/Officials/Votes, Daily Brief/Alerts/Actions/Badges, and Privacy/Support in the [device QA worksheet](t06-physical-iphone-qa-2026-09-17.md). Ed Case's separate returning-member tenure count needs source diagnosis but does not displace the ordered walkthrough. Run deletion only with an assigned disposable account and exact approval. |
| T07 | Subscription baseline and transition matrix — **source implementation and execution plan committed; sandbox proof pending.** Candidate `3dbba3a` adds provider-authoritative Apple reconciliation, Notifications V2 receipts, Team acknowledgement/restoration safeguards, and fixtures. Its source/branch evidence passes, but it has no signed-device or Apple sandbox result. | Owners: Tyler for account/action approval; Codex for validation; Apple sandbox timing is external. Estimate: 1–2 working days after an exact signed candidate, excluding propagation wait. Risk: high. Execute every applicable row in the [App Store sandbox QA matrix](app-store-sandbox-qa-matrix-2026-09-11.md), including purchase, restore, renewal, retry/grace, expiry/refund, notification replay/order, duplicate ownership, Team transitions, and deletion separation. |
| T08 | YouTube channel and first real Daily Brief — public channel `https://www.youtube.com/@CapitolWonk` is created and verified, the CapitolWonk banner is uploaded, and its canonical URL is configured in candidate `3dbba3a` with `episodes` empty. Matching Preview smoke retains the intentional placeholder, exposes the real Subscribe target, and loads no iframe. | Owners: Tyler for first-video content/access and separate approval; Codex for integration/QA; YouTube is external. Estimate: 1–2 working days after video inputs plus platform wait. Risk: medium/high. No video, embed/captions/transcript/sources, production or physical-iPhone verification, real-player runtime privacy evidence, or launch-scope decision exists. Replace the channel's Daily Brief destination with the Apple App Store product URL when that stable public URL exists; add Google Play later. This is not silently a launch blocker; confirm launch scope. |
| T09 | Release assets, App Privacy, provider evidence, and exact upload decision — **launch work continues; first-party operator activation is conditional post-launch.** Production Batch A and default-off source are complete, and the configured dedicated mailbox is the launch privacy-intake channel. The later ambient-privilege preflight failed closed; the principal-ACL inventory never ran and must not be retried. The exposed Neon owner credential was directly reset by Tyler and containment is complete; see the [containment record](neon-owner-credential-containment-2026-09-15.md). First-party intake, operations, monitoring, retention, deletion, and the adapter remain off. The [provider/PITR checklist](backup-pitr-provider-retention-evidence-2026-09-11.md), [privacy/assets packet](app-privacy-release-assets-prep-2026-09-12.md), [capture manifest](app-store-assets/capitolwonk-capture-manifest-2026-09-13.md), and [privacy-rights decision](privacy-rights-intake-decision-2026-09-12.md) hold the remaining launch evidence. | Owners: Codex for source-only App Privacy/metadata/asset reconciliation and read-only evidence; Tyler for every questionnaire publication, remote-asset edit, upload, distribution, submission, or release. Risk: high until App Privacy/assets, provider evidence, signed QA, and release gates close. Any future production privacy migration, ACL, role, credential binding, activation, destructive QA, or provider mutation requires exact approval and is not launch-critical under the current scope. |
| T10 | Tester distribution and review — pending; no TestFlight upload, invitations, public link or review submission has occurred for the current candidate. | Owners: Tyler for every exact distribution/review approval; Codex for preparation; Apple processing/review is external. Estimate: 0.5–1 working day hands-on plus unknown processing/review. Risk: high. Confirm processed build, audience, metadata, support and feedback coverage; schedule outside October 2–6. |
| T11 | **October 30 launch target — planned, not authorized.** No exact current signed candidate or Apple approval evidence exists. | Owners: Tyler for final go/no-go/release authorization; Codex for evidence and options. Estimate: 0.25–0.5 working day decision preparation; external wait excluded. Risk: high until T04–T10 clear. Confirm correct build/metadata, final regression, accepted-risk record and support/monitoring coverage; do not move the date or bypass gates silently. |

## Carried scope decisions and deferred work

- Main CapitolWonk TestFlight path stays first. Do not reopen completed web/branding text work or repeat the applied daily-editions migration.
- Retain unresolved v1 scope/readiness decisions from the older roadmap: provider-backed rate limiting, final auth-email delivery/volume, civic-data freshness/fallback labeling, source-grounded AI/live-provider verification where needed, Senate vote coverage and any additional data-sync scope. Reconcile each as done with evidence, required before release, accepted risk or explicitly deferred; do not silently drop them or rerun broad writes.
- Daily Brief email/push delivery and any authenticated publishing editor/feed automation remain post-launch candidates. Push-notification v1 scope is unconfirmed; no new provider/scheduler/sends activated.
- Standalone Supreme Court sister-app planning starts **after the main app reaches TestFlight**. State legislation remains a later update inside the main app, tentatively early 2027; this is a planning direction, not a committed release date.
- Historical Round 1–3 tester exports remain non-distributable. Regenerate approved current materials only after the new branding and actual release scope are settled.

## Required EOD maintenance

1. Reconcile every task with evidence: completed, in progress, blocked (with owner/decision), pending or explicitly deferred. Carry all unfinished IDs into the next handoff; never erase a blocker because a session ended.
2. Record actual completion dates and links/commit evidence. Separate implemented, deployed, device-verified and externally approved states.
3. Update remaining hands-on effort, external wait time, next checkpoint/working target and confidence when evidence supports an estimate. Preserve the previous estimate and explain date changes; do not silently reset the baseline.
4. Report ahead/on track/behind only against an established baseline. If ahead, pull forward the next already-scoped, dependency-ready task and preserve recovery/QA contingency; do not expand scope or bypass approvals to consume the time gained.
5. Recalculate around October 2–6 and any updated availability. Move owner-dependent gates outside the buffer and explain any effect on forecast dates; do not automatically treat five calendar days as five working days.
6. Keep this ledger, the latest dated EOD, `Current Status.md` and `Next Steps.md` aligned. Every EOD must identify tomorrow's first task, all carryovers, schedule changes and the single safest next action. Keep the user-set October 30 launch target visible, compare the forecast with it, and show remaining review/availability contingency. Any proposed target change requires Tyler's decision.

## Change log

- **September 3, initial closeout:** web milestone completed; logo refresh added as first September 4 task. Consolidated prior TestFlight/service/subscription and post-launch tasks without declaring outstanding checks complete. Added October 2–6 availability contingency. No fixed launch baseline was initially found in prior documents.
- **September 3, user clarification:** Tyler set **October 30, 2026** as the launch date. Replaced the undated baseline with that target, added T11 and proposed intermediate checkpoints backward from launch, retaining the October 2–6 buffer and October 20–29 review/rework contingency. Remaining-effort re-estimation is still due September 4; schedule performance is not yet rated ahead/on-track.
- **September 10 resumption and release:** restored the September 3 documentation checkpoint, found no tracked product evidence for September 4–10 and rated the plan behind its first internal checkpoint. Tyler clarified that the logo stays; completed the shared-wordmark `CE` removal at `6ce9d5b`, then released it after exact approval on production source `7ec68bc`. CI, Vercel and live smoke passed. Revised T02–T07 planning windows without moving October 30, preserved October 2–6 and October 20–29, and recorded low confidence/material risk until the audit, signing, device and subscription gates are evidenced.
- **September 10 July 31 evidence reconciliation:** recovered pushed but unmerged July 30–31 evidence that Tyler had already approved and verified the recursive Sentry geography scrub. Marked T02 complete for new events without rerunning a probe or touching protected configuration, reduced the preliminary hands-on range by the completed T02 estimate, and advanced the active queue to T03. Also restored the superseding Apple Support signing hard stop, physical-device preparation and protected native-Sentry build evidence so T04–T06 retain their correct boundaries.
- **September 10 T03 audit:** completed the registry-backed production and full audits with exact Node `22.22.3` and pnpm `9.15.9`, without installing or changing packages. Recorded 15 production findings—2 critical, 11 high and 2 moderate—and 18 full-graph package-version findings representing 17 distinct advisory IDs. Marked the baseline audit evidence complete but the then-current T03/release gate failed, retained July's accepted subset as historical but insufficient for the expanded result, added the required Tyler remediation-versus-exception decision, and revised remaining hands-on effort from 8.5–16 to 9–18 working days. The later entries record its remediation and closure.
- **September 10 T03 approved remediation:** after Tyler approved the narrow path, aligned the Next toolchain on `15.5.25` and used a targeted Node 22/pnpm 9 lock refresh that moves every compatible vulnerable transitive. Normal resolution also updates compatible packages within those dependency subtrees; the direct manifest changes only the three Next pins. The frozen install, native/true-WASM builds, 38 application/readiness checks, optimized HTTP/image smoke and unsigned iOS Simulator Release build pass. Production/full audits are now 0 critical, 2 high and 2 moderate, all in Next's exact nested PostCSS. Reduced the preliminary remaining range from 9–18 to 8.25–15.5 working days; kept T03 open for branch evidence and Tyler's exact current-residual decision.
- **September 10 T03 closure:** pushed exact candidate `f4f04de`; GitHub Actions run `34518247389` and matching Vercel deployment `mep97jz9t5Hncx1t2mRBeTytDqut` passed. Authenticated preview smoke reached 14 app pages/redirects and both image-optimizer paths; anonymous bulk smoke was correctly intercepted by Deployment Protection. Tyler explicitly accepted the exact current Next-pinned PostCSS two-high/two-moderate residual for this candidate. Marked T03 complete, retained the exception/re-audit condition, advanced the active queue to T04 read-only reconciliation and reduced remaining hands-on effort to 8–15 working days.
- **September 10 T04 current-state reconciliation:** confirmed zero usable code-signing identities, one current matching Xcode-managed App Store profile, stable Xcode 26.6 selected, Xcode 27 beta installed/unselected and the paired iPhone disconnected. Located the existing open Support case; Tyler confirmed no reply and explicitly approved a sanitized correction with the private case ID supplied only to Apple. Apple confirmed receipt. T04 remains blocked pending guidance, the signing freeze remains in force and the 8–15-day hands-on range is unchanged because external Support wait time is excluded. An unrelated unexplained lockfile downgrade was quarantined and, after Tyler's explicit approval, restored to the committed T03 version without entering any commit.
- **September 10 App Privacy correction preparation:** audited current App Store answers against source, Sentry configuration, the native SDK privacy manifest and resolved Sentry Cocoa implementation. Prepared five provisional additions—Emails or Text Messages, Device ID, Crash Data, Performance Data and Other Diagnostic Data—while retaining the nine existing types and No tracking for the current no-video/no-analytics configuration. Native installation identifiers require linked diagnostic answers unless stripped and verified absent. Updated the public policy source and readiness checks, recorded archive/runtime/WKWebView/provider/deletion/YouTube gates, and reduced remaining T09 preparation by roughly 0.25–0.5 day. No production or App Store Connect state changed.
- **September 10 account-deletion implementation:** converted the local request-only flow into immediate atomic account erasure with transactionally captured Team-member/legacy-Stripe cleanup jobs, linked relational/raw-store cleanup, post-delete zero-row assertions, a deidentified completion audit, all-session/auth-cookie clearing, and fenced current-device storage clearing. Exact-ID persistence prevents stale recreation/email remap; signed Stripe events use timestamp and current-state checks, including deleted-user cleanup before acknowledgement; terminal/missing cleanup results complete while transient errors retry; concurrent member pauses serialize against deletion and attempt provider compensation on failure; hashed rate-limit subjects are cleared; and completion requires an explicit receipt while ambiguous results remain unconfirmed. Successful cleanup jobs are erased and pending jobs remain only for retry. The three migrations include strict known-sentinel conversion and unexpected-orphan abort behavior. Production migration/deploy, task scheduling/age monitoring, real-Postgres concurrency, live Stripe and physical-device multi-tab QA, backup/PITR tombstones, and provider removal procedures remain open.
- **September 10 final EOD and restart integrity:** confirmed `2e4e04b` was synchronized with the branch remote before final review, the accepted dependency lock graph remained intact, and the privacy/deletion change set survived the restart without detected corruption artifacts. TypeScript, full ESLint, Prisma validation, deletion/cleanup/persistence/readiness, billing local-prep, feedback, TestFlight local-prep/mobile UI, launch copy, iOS bridge and diff checks passed.
- **September 10 final branch checkpoint:** Tyler authorized the final checkpoint on `codex/logo-refresh-sept10`. Complete tracked/untracked audits found the privacy/deletion and channel-only paths in scope with no secret, private case ID, lockfile drift, or unrelated file. Final review removed an unprovable direct-navigation deletion claim and made Stripe customer cleanup paginate every subscription page, failing closed on malformed or later-page errors; fixtures cover both. The reviewed payload was committed as `871ba4e`. Its first exact-head CI run exposed one strict unused-parameter binding; the source correction in `a6cb1da` removed only that binding while preserving the accepted input shape, its accompanying documentation edits reconciled the checkpoint wording, and the exact strict TypeScript check passed locally.
- **September 10 final branch verification:** exact source candidate `a6cb1da` synchronized with the remote branch. GitHub CI run `34557134361` passed, matching Vercel Preview deployment `6mKbaWGJ2jdRDbNRaB1K8ZMYKh8L` reached Ready, and authenticated `/brief`, `/privacy`, and `/account-deleted` preview smoke passed. The branch evidence gate is complete. Production and all migration/configuration/destructive/provider/App Store/upload/distribution/release gates remain unchanged; September 11 begins with production migration history read-only.
- **September 10 channel-only YouTube update:** verified the public `CapitolWonk` / `@CapitolWonk` channel in the authenticated browser and configured `https://www.youtube.com/@CapitolWonk` in exact source candidate `a6cb1da` with `episodes` empty. The app retains the honest placeholder, exposes the real Subscribe target and loads no player. Updated privacy/publishing wording for outbound navigation; local Daily Brief, editorial, dated-edition, launch-copy, feedback, mobile UI, TypeScript, targeted lint, browser and diff checks pass. Matching authenticated HTTPS Preview smoke also confirms the placeholder, exact Subscribe target, and no player. No YouTube setting, video, production deployment, privacy publication or forecast changed; T08 remains open for content, real-player/runtime/physical-device evidence and launch scope.
- **September 11 working-candidate reconciliation:** the Apple lifecycle, Team transition, deletion/retention activation, telemetry minimization, fourth-migration, blank-account isolation, and QA-matrix work is frozen in committed branch candidate `3dbba3a`. The frozen production/full audits report no known vulnerabilities; release-source, strict TypeScript, local lint, Prisma generation, and the repeated exact four-migration read-only preflight pass. Parent `bbe63e4` exact-head CI exposed four unused demo paths; `3dbba3a` removes them and passes [exact-head CI `34668039916`](https://github.com/Tylerandersongates/Capitol-Ledger/actions/runs/34668039916). Matching Vercel deployment `6K9Xd24c4Pg4Nz1pRLorNzo6MxGb` reached Ready and targeted anonymous `/dashboard`, `/search?type=bills`, and `/brief` smoke passed at that time. Provider/sandbox/device QA and all approvals remain open. The September 12 security review supersedes this entry's original EOL-only `jsrsasign@11.1.5` characterization with the known issue #447 gate in the [September 12 decision packet](jsrsasign-risk-decision-2026-09-12.md).
- **September 11 final EOD and memory reconciliation:** pushed documentation/asset checkpoint `d662aae` after preserving the verified source candidate and uploaded 2560×1440 YouTube banner. The 8 GB host currently reports about 35% memory free, no abandoned CapitolWonk Next worker, and approximately 2.28 GB across 233 Codex/ChatGPT processes. The prior 512 MB Node heap was a deliberate safety ceiling, not an OS address-space limit. Exact-head CI/Vercel are the working build workaround; a full Codex restart before any optional 1 GB local build retry should recover the accumulated renderer/helper memory. Reduced estimated remaining hands-on effort from 8–15 to 7–13 working days because source/evidence/channel work is complete; external Apple/provider waits and QA rework remain excluded.
- **September 12 checkpoint and provider evidence:** confirmed local/remote branch checkpoint `cd09453`, exact source boundary `3dbba3a`, passing CI `34668039916`, matching Ready Preview `6K9Xd24c4Pg4Nz1pRLorNzo6MxGb`, and production `7ec68bc`. The public anonymous smoke remains recorded historical evidence; current fresh public requests are intercepted by Deployment Protection, while app-anonymous behavior behind the gate remains correct. Authenticated read-only provider inspection captured Neon’s six-hour history/no snapshots, Vercel Pro plus Observability Plus/no project drains or webhooks, both Sentry projects’ current scrub/IP/geography controls and organization-level gaps, and Resend Pro/no webhooks/no recent sends. Exact delivery modes, runtime samples, restore/removal exercises, and the recoverability decision remain open. No provider state changed.
- **September 12 security/privacy/assets and Apple reply preparation:** reclassified `jsrsasign@11.1.5` from an EOL-only decision to a known OCSP-freshness release gate based on Apple issue #447. Recommended waiting for an official fix through September 25 while preparing a source-free fallback/test specification; no package, source, or test changed. Preserved the prior **7–13-day** product/release baseline but added verifier-remediation scenarios: about **8–15 total** for a fully sufficient official package, **9–17** for an official fix plus local controls, **10–18** for a reviewed backport plus controls, or **11–20** for replacement. Preserved the provisional fourteen-type App Privacy matrix, identified default-off deletion and Sentry privacy-rights/feedback intake blockers, and recorded exact stale listing/review asset dimensions and hashes plus a sanitized recapture plan. A read-only Apple mailbox check found no substantive reply after the September 10 acknowledgment. T04 remains frozen; if still unanswered September 17, prepare one sanitized same-case escalation for Tyler’s approval without sending or duplicating it. October 30 remains unchanged but at higher schedule risk.
- **September 12 approved next steps executed:** Tyler approved one Neon child plus aggregate/read-only Phase 1 checks, deployment-target reconciliation, and use of the existing child connection solely in memory. The branch is isolated and queryable. A database-size inventory exposed a distinct literal `Capitol%20Ledger` database at 139 MB; Neon's authenticated Tables surface verified its expected application schema, 12 finished migrations, the known resolved retry, and exactly five pending candidate migrations. An exact aggregate query verified `public`, 29 tables, and zero deletion-request rows across open, resolved, completed, qualifying, and improperly linked categories. Phase 1 passes with `no completed-deletion watermark present`; automatic one-day cleanup and later phases remain open. The credential was not printed, persisted, changed, or created, and the temporary relay was deleted. Fail-closed database-target checks require the literal database name, `public` schema, and three control tables before future database operations. A one-time read-only branch-expiry follow-up is scheduled for September 13 at 12:00 PDT. Tyler selected Option 1, approved local source/schema/test work, and then approved the candidate freeze. Candidate `92b61b970423ce33dc32f6a2f0a9b72a6d3e2e86` now contains the default-off first-party privacy queue, validated blank-by-default mailbox fallback, corrected Support/Privacy/Sentry boundary, deletion cascade/postcondition, fifth migration, target guard, and focused checks. Its offline frozen install, production/full audits, Prisma validation/generation, strict TypeScript, full lint, local-preparation release-source checks, production build, diff checks, PR #8 CI #315, refreshed Ready Preview, and targeted privacy-fallback gate-off smoke pass through documentation checkpoint `3b78e02`. The strict candidate wrapper remains blocked on absent protected runtime/App Store values. The branch is pushed and PR #8 is open; no merge, production migration, production deployment, configuration, activation, provider message, synthetic deletion, or release action occurred.
- **September 12 email prerequisite and candidate refresh:** Resend verified `capitolwonk.com`; Porkbun retained the existing web/forwarding records and added the required sending records plus four owner-directed aliases. Vercel Production/Preview now contain the CapitolWonk auth sender, Brief sender, and privacy-contact values; the existing sending secret was preserved. The same production source was redeployed Ready and live `/privacy` smoke passed. No test message was sent. The documentation-only candidate refresh re-ran both registry-backed audits with no known vulnerabilities and the complete local-preparation `release-source:check` successfully. The strict `release-candidate:check` reached only the expected eight missing protected production/database/Apple values in this intentionally secret-free worktree. Documentation checkpoint `3b78e02` is pushed; [GitHub CI #315](https://github.com/Tylerandersongates/Capitol-Ledger/actions/runs/34720190325) passed install, TypeScript, brand consistency, lint, feedback, both audits, release-source safeguards, and the optimized build. The refreshed Vercel Preview is Ready and targeted `/privacy/request` smoke displays the verified privacy fallback while database intake stays off. Candidate `92b61b9` remains unmerged, and no database or App Store state changed.
- **September 12 final EOD:** created the September 12 handoff with tomorrow's first task set to the scheduled one-time read-only Neon child-expiry verification at 12:00 PDT. Carried T01–T11 and every deferred scope item forward, preserved the October 2–6 availability buffer and October 20–29 review/rework contingency, and retained the October 30 target as low-confidence/materially at risk. No source, dependency, production, database, provider, Apple, signing, upload, distribution, submission, or release state changed during closeout.
- **September 13 Neon cleanup completion:** the scheduled read-only check found `1 / 5000 Branch` and only protected default `production`; the temporary restore child and its compute expired automatically. A final read-only dependency/target check showed 29 public application tables and live production `Bill`, `Vote`, and `MemberVote` SELECTs under literal `Capitol%20Ledger`. Spaced-name `Capitol Ledger` had zero public tables, only catalog/introspection history, and no tracked runtime reference outside the guard's negative self-test. Tyler separately confirmed permanent deletion of exactly the 7344 kB spaced-name database. Neon now lists only retained `neondb` and application database `Capitol%20Ledger`; the post-delete production read smoke passed. No migration, restore, application-row write, candidate merge/deployment, or activation occurred.
- **September 13 handoff refresh and Apple parallel queue (historical checkpoint):** live read-only verification found the candidate branch and open PR #8 synchronized at documentation checkpoint `c96662a`; CI #318 and its matching Vercel Preview passed. Apple Support still had not replied, and Tyler planned to call September 14. The signing/device freeze remained intact. The exact five-migration promotion packet, privacy operations/single-owner contingency, read-only SQL companions, and local capture manifest were prepared but unapproved. The former Phase 1 child expired, so the next execution proposal remained a separately approved replacement temporary child/compute with protected credential handling, fresh baseline/target checks, expiry/cleanup ownership, and Phase 2—not production Batch A. No production, database, provider, App Store, signing, device, upload, distribution, submission, or release action was authorized by that checkpoint.
- **September 13 current PR verification and production compatibility finding:** open, unmerged PR #8 advanced through documentation/preparation head `0c09abc0523e2f4de16f55f4d6d2465d27f4abfa`; exact-head CI #321 and matching Ready Vercel Preview `7d1i8rinVS8iUhUTPUyba4DMwUfy` pass, while `main` remains `7ec68bc`. Read-only source reconciliation found that unchanged production can still write legacy Stripe Team sentinel `team-owner-upgrade`, which migration 3 would make foreign-key-invalid. Production Batch A is therefore no-go pending a separately approved replacement child/Phase 2, mocked reproduction, and compatible-source or complete source/traffic-quiescence sequence. Privacy intake and all Apple/signing/device/release gates remain off; October 30 remains low-confidence and materially at risk.
